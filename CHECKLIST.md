# CHECKLIST — **Tassullo Design System 2.0**

> **Fonte di verità dell'avanzamento.** Stati: `TODO | IN_PROGRESS | BLOCKED | REVIEW | DONE`. Si aggiorna a ogni cambio di stato e al termine di ogni attività significativa. Il dettaglio dei task (Prompt, File, Accettazione completa) è in `PIANO.md`, che qui non si duplica; il diario è `WORKLOG.md`.
> Un task = una sessione Claude Code, salvo indicazione contraria. **Totale al 2026-09-22: 74 righe = 77 sessioni**: `M3.3` vale 2 sessioni e `M3bis.5` 3. Come si è passati dalle 41 righe del piano originale — FASE 3bis, FASE 4ter con le sue code, M5.0a–e — sta in `PIANO.md` e in `WORKLOG.md`.
> **La colonna del criterio è una sintesi, e ha un tetto.** Il verdetto, i due o tre numeri che contano, un rimando. Ogni riga di questo file sta entro **500 caratteri**, e `npm run check:checklist` lo verifica. Il ragionamento, le misure, le trappole e le rettifiche stanno in `WORKLOG.md` e in `docs/DECISIONI.md`: una riga che non si legge smette di essere una fonte di verità.
> Nessuna colonna Responsabile: il progetto è condotto da una sola persona (Francesco) con Claude Code. Se un giorno lavorassero in due, si aggiunge qui la colonna e si adotta la regola dei worktree di Anagrafe.

## FASE 0 — Scaffold e fondamenta (5 sessioni)

Gate: `npm run storybook` mostra una primitiva shadcn di prova con Tailwind v4 attivo e i tre interruttori (tema, densità, viewport); i quattro documenti di conduzione esistono; git locale inizializzato. **Gate superato il 2026-09-07** (commit `a435201`).

| Attività | Stato | Dipendenze | Criterio di accettazione (sintesi) |
|---|---|---|---|
| M0.1 Documenti di conduzione e struttura | DONE | | `CHECKLIST.md`, `WORKLOG.md`, `CLAUDE.md` coerenti con `PIANO.md`; tutti i task elencati; `CLAUDE.md` con tutte le regole del piano |
| M0.2 Scaffold Vite + Tailwind v4 + `shadcn init` | DONE | M0.1 | `npm run dev` con utility Tailwind attiva; `shadcn add button` compila **in variante Base UI**, meccanismo di selezione scritto in `docs/DECISIONI.md` |
| M0.3 Storybook: la style guide 2.0 | DONE | M0.2 | `npm run storybook` apre l'indice con una story di prova; i tre interruttori funzionano; `build-storybook` produce `storybook-static/` servibile |
| M0.4 MCP shadcn e prerequisiti d'ambiente | DONE | M0.2 | dall'MCP si elencano i componenti shadcn **e** gli item del registry Tassullo locale; versioni Node/npm annotate in `docs/DECISIONI.md` |
| M0.5 Git locale | DONE | M0.1..M0.4 | `git log` col commit iniziale, `git remote -v` vuoto (D4), `CLAUDE.md` rivisto su ciò che le 4 sessioni hanno prodotto davvero |

## FASE 1 — Tema Tassullo (5 sessioni)

Gate: la pagina Palette mostra tutte le coppie token nelle 4 combinazioni (light/dark × normale/touch); `check:contrast` passa su tutte le coppie `X`/`X-foreground`.
**Superata** con M1.3–M1.5: `check:contrast` verde su **48 coppie**, le story `Tema/Palette` (56 token, valore letto dal DOM) e `Tema/Densità`, e il tema che esce dal repo come item con **installazione provata end-to-end** su un'app vuota. Riaperta e richiusa da **M1.6**, che ha riallineato la scala tipografica → `WORKLOG.md`, voci M1.3–M1.6.
Dipendenze di fase: FASE 0.

| Attività | Stato | Dipendenze | Criterio di accettazione (sintesi) |
|---|---|---|---|
| M1.1 Mappa dei token e script di conversione | DONE | M0.2 | `PIANO.md` §2bis scritto; `npm run check:contrast` gira e **fallisce** se si forza `--primary-foreground` a bianco |
| M1.2 `tassullo-theme.css`, modalità chiara | DONE | M1.1 | contrasto verde (24/24); `bg-primary text-primary-foreground` rende arancione con testo nero, verificato a video |
| M1.3 Modalità scura (chiude D2) | DONE | M1.2, M0.3 | contrasto verde 48/48 (24 per modalità); story `Tema/Palette` con le due palette affiancate e `docs/img/M1.3-palette-chiaro-scuro.png`; **D2 chiusa** |
| M1.4 Densità touch | DONE | M1.2, M0.3 | interruttore verificato senza ricaricare; default **48px** (xs 36, sm 42, lg 54, icona 24), misure lette dal DOM nella story `Tema/Densità`; due eccezioni annotate (sidebar → M2.5, `text-[0.8rem]` di `sm` → M2.1) |
| M1.5 Il tema come item di registry | DONE | M1.3, M1.4 | `registry validate` verde su 2 item; **prova d'installazione end-to-end** su app Vite vuota, con `vite build` che compila `text-*` come `var()` e i due blocchi densità; pagina Palette coi 56 token, valore letto dal DOM e click-to-copy; `cssVars`/`css` scartati con la prova (`DECISIONI.md` §11) |
| M1.6 Riallineamento della scala tipografica | **DONE** 2026-09-10 | D16 chiusa | ✔ attuata **D16**: scala in vigore `xs 12 / sm 13 / base 15 / lg 16 / xl 19 / 2xl 27 / 3xl 31`, **zero gradini nostri** — `--text-md` fuso in `sm`, `--text-title` diventato `2xl`, `3xl` tarato accanto. → dettaglio in `WORKLOG.md`, voce **M1.6** |

## FASE 2 — Primitive (9 sessioni)

Gate: M2.9 verde su tutto il set (contrasto, tastiera, densità, dark), con axe-core in CI. **Superato il 2026-09-10**: `npm run test:a11y`, **852 scansioni, 0 violazioni**, dentro `npm run check` e in `.github/workflows/gate.yml`. Dipendenze di fase: FASE 1; ogni primitiva in variante **Base UI** (D9).

**Due rilievi restano aperti.** Gli altri tre sono evasi da M2.9, e la loro storia — coi conti che cambiavano quando la misura imparava ad aprire i popup, e le due rettifiche di falsi positivi — sta in `WORKLOG.md` (M2.6, M2.9) e in `docs/DECISIONI.md` §27.

1. **Il contorno dei controlli sta sotto 3:1** (WCAG 1.4.11), su tutto il set e in entrambe le modalità: `border-input` misurato **1.27 in chiaro, 1.36 in scuro**. Non viene dal ri-stile, è il valore ereditato dal v1, e `check:contrast` non lo vede perché verifica **solo coppie di testo**. È una decisione di **palette**, in `hex-to-oklch.ts`.
2. **Filtri: sorvolato e acceso sono lo stesso grigio** — noto e **accettato** (Francesco, 2026-09-09): tingere ogni filtro acceso col brand gli toglierebbe il potere di segnalare. **Si riprende in FASE 4**, su una barra filtri vera.

| Attività | Stato | Dipendenze | Criterio di accettazione (sintesi) |
|---|---|---|---|
| M2.1 Fondamenta (`button`, `badge`, `separator`, `skeleton`, `spinner`, `avatar`, `kbd`, `button-group`, `typography`, `lib/utils.ts`) | DONE | M1.5 | `button` in tutte le varianti/stati nelle 4 combinazioni; `typeset` riproduce la scala v1 in **Inter** e fissa la regola delle cifre; **chiusi i 4 rilievi su `button`** più l'*incomplete* su `ghost` e il `text-[0.8rem]` di `sm` → dettaglio in `WORKLOG.md`, voce **M2.1** |
| M2.2 Form (`field`, `input`, `input-group`, `label`, `textarea`, `select`, `checkbox`, `switch`, `radio-group`, `slider`) | DONE | M2.1 | form di prova con **14 fermi di tabulazione**, tutti in ordine di DOM, tutti con nome accessibile e anello di fuoco visibile; **axe 158 scansioni (79 story × 2 modalità), 0 violazioni**. → dettaglio in `WORKLOG.md`, voce **M2.2** |
| M2.3 Overlay (`dialog`, `alert-dialog`, `drawer`, `sheet`, menu, `popover`, `tooltip`, `sonner`, `command`) | DONE | M2.1 | **11 primitive, 32 item, `registry validate` verde.** Fuoco intrappolato ed `Esc` verificati **in un browser vero** su tutti e 5 i modali: `Invio` apre, 8 `Tab` non escono, `Esc` chiude, il fuoco torna al grilletto (il `popover` non intrappola, ed è voluto: non è modale). → dettaglio in `WORKLOG.md`, voce **M2.3** |
| M2.4 Contenuto (`card`, `tabs`, `table`, `alert`, `empty`, `accordion`, `collapsible`, `scroll-area`, `resizable`, `progress`, `aspect-ratio`, `carousel`) | DONE | M2.1 | **12 primitive, 44 item, `registry validate` verde.** Le quattro varianti semantiche di `alert` si fanno con `className`, non con quattro nomi di variante: scala 4bis ferma al gradino 1, e la strada opposta è stata provata e misurata → dettaglio in `WORKLOG.md`, voce **M2.4**, e `docs/DECISIONI.md` §25 |
| M2.5 Navigazione (`sidebar`, `breadcrumb`, `pagination`) | DONE | M2.1 | **3 primitive + 1 hook, 48 item, `registry validate` verde.** Sidebar antracite già dai token `--sidebar-*`, e lo stato attivo del preset **coincide alla lettera** con la regola del v1 (`bg-sidebar-accent` + `sidebar-accent-foreground`): scala 4bis ferma al gradino 1 su tutte e tre. → dettaglio in `WORKLOG.md`, voce **M2.5** |
| M2.6 Filtri e selezione (`toggle`, `toggle-group`, `combobox`, `multi-select`/`tag-input`) | DONE | M2.2, M2.3 | **3 primitive, 51 item, `registry validate` verde.** Il piano ne chiedeva quattro: `combobox` **non si compone più** da `command` + `popover`, è un item shadcn suo su Base UI, e con `multiple` porta **anche** il multi-select e il tag-input — pillole comprese. → dettaglio in `WORKLOG.md`, voce **M2.6** |
| M2.7 Date (`calendar`, `date-picker`) | DONE | M2.3 | **1 primitiva, 52 item, `registry validate` verde.** **L'eccezione a Base UI di D9 non si pone**: Base UI non ha un calendario e shadcn ne spedisce uno solo, su `react-day-picker` + `date-fns` — nessun secondo candidato da valutare, quindi niente da motivare. → dettaglio in `WORKLOG.md`, voce **M2.7** |
| M2.8 Dati (`chart`, palette categorica) | DONE | M2.4 | **1 primitiva, 53 item, `registry validate` verde.** `--chart-1..5` non sono cinque colori scelti ma **cinque pioli di una scala di chiarezza**, e il passo non è deciso: è **1.4935:1**, quello che `--primary` e `--success` hanno già fra loro (misurato). → dettaglio in `WORKLOG.md`, voce **M2.8** |
| M2.9 Gate di fase: audit del set | DONE | M2.1..M2.8 | **852 scansioni, 0 violazioni.** `a11y.test = 'error'`; `addon-vitest` + Playwright/Chromium; `npm run test:a11y` fa **quattro passate** — {chiaro, scuro} × {popup chiuso, aperto} — e **dichiara quali popup apre** (47/48). → dettaglio in `WORKLOG.md`, voce **M2.9** |

## FASE 3 — Blocchi applicativi Tassullo (11 sessioni)

Gate: M3.10 — una pagina reale di Anagrafe ricostruita in Storybook **usando solo i blocchi**, senza CSS di pagina.
Dipendenze di fase: FASE 2. Ogni blocco dichiara i propri `registryDependencies`.

| Attività | Stato | Dipendenze | Criterio di accettazione (sintesi) |
|---|---|---|---|
| M3.1 `tassullo-app-shell` | **DONE** 2026-09-10 | M2.5 | ✔ quattro celle viewport × densità misurate in Chromium: larghezza utile **1148 / 1008 / 343 / 327px**, caratteri per riga **165 / 136 / 49 / 44**; prima misura di D10 a verbale (`docs/DECISIONI.md` §29). → dettaglio in `WORKLOG.md`, voce **M3.1** |
| M3.2 `page-header` | **DONE** 2026-09-10 | M3.1, M2.5 | ✔ **2 blocchi, 56 item, `registry validate` verde.** La pagina *dichiara* percorso e azioni, un **portale** le rende nella fascia: il guscio si monta una volta sola. Soglie `@container` sulla fascia, una riga sola ovunque. → `WORKLOG.md` M3.2 e code |
| M3.3 `data-table` (**2 sessioni**) | **DONE** 2026-09-11 | M2.4, M2.6 | ✔ **3 blocchi, 57 item, 916 scansioni / 0 violazioni.** TanStack Table v9: colonne loro, contorno nostro. Sessione 2: sotto soglia si scorre, con `bloccaPrimaColonna`. **D17 chiusa**. Nuovo item `toni` (58). → `WORKLOG.md` M3.3 (1 e 2 di 2) e code, `docs/DECISIONI.md` §33 |
| M3.4 `form-field`, `confirm-dialog`, `responsive-dialog` | **DONE** 2026-09-11 | M2.2, M2.3 | ✔ **3 blocchi, 61 item, 972 scansioni / 0 violazioni.** Dialogo sopra 768px, cassetto sotto: la soglia è `useIsMobile()` di shadcn, senza prop per spostarla; misurata su sei celle. Dipendenza nuova: `react-hook-form`. → `WORKLOG.md` M3.4 |
| M3.5 Stati (`empty-state`, `page-skeleton`, `error-state`, `toast-con-annullo`) | **DONE** 2026-09-15 | M2.4, M2.1 | ✔ **4 blocchi, 65 item, 1016 scansioni / 0 violazioni.** `error-state` è la primitiva `empty` tinta `destructive-subtle`. **D18** chiusa qui: annullo differito lato client, provvisorio — da rivedere con Roberto. → `WORKLOG.md` M3.5, `docs/DECISIONI.md` §35 |
| M3.6 `file-upload` | **DONE** 2026-09-15 | M2.2, M2.4; D8 | ✔ **1 blocco, 66 item, 1028 scansioni / 0 violazioni.** D8: `react-dropzone`. Provato in Chromium con 5 file, uno fallisce con errore tradotto; `Tab` arriva su un bottone vero. → `WORKLOG.md` M3.6 |
| M3.7 `pdf-preview` e `version-timeline` | **DONE** 2026-09-15 | M2.4; D8 | ✔ **2 blocchi, 68 item, 1044 scansioni / 0 violazioni.** D8: `react-pdf`, worker locale e non da CDN; un PDF reale aperto, sfogliato e ingrandito in Chromium. `version-timeline` con selezione a coppia per il confronto. → `WORKLOG.md` M3.7 e code |
| M3.8 `rich-text-editor` | **DONE** 2026-09-15 | M2.2; D8 | ✔ **1 blocco, 69 item, 1060 scansioni / 0 violazioni.** D8: `@tiptap/react`. La barra ridotta è lo **schema** del documento: un incolla da Word perde ciò che lo schema non ammette. **D11 chiusa**: non si adotta `typeset`. → `WORKLOG.md` M3.8 e coda, `docs/DECISIONI.md` §36 |
| M3.9 `diff-view` e `split-view` | **DONE** 2026-09-15 | M2.4; D8 | ✔ **2 blocchi, 71 item, 1084 scansioni / 0 violazioni.** D8 chiusa: `diff` (jsdiff). `split-view` impila sotto 448px **di contenitore** e scorre in sincronia per frazione. → `WORKLOG.md` M3.9 e coda, `docs/DECISIONI.md` §31 |
| M3.10 Gate di fase | **DONE** 2026-09-15 | M3.1..M3.9 | ✔ `stories/PaginaProdotti.stories.tsx`: `app-shell` + `page-header` + `data-table` + `empty-state`, zero CSS di pagina; `check` verde sui cinque gate. Coda: `perPagina="infinito"` (**D19**) e `nomeRighe`. → `WORKLOG.md` M3.10 e code, `docs/DECISIONI.md` §37 |

## FASE 3bis — Tabelle avanzate, niko-table (10 sessioni)

Gate: `pagina-lista` mostra almeno un esempio di ciascuna nuova capacità, senza CSS di pagina; `check` verde sui cinque gate.
Dipendenze di fase: M3.3, M4.2. Si porta (si riscrive) il pattern da niko-table, non si installa il pacchetto — dettaglio in `docs/DECISIONI.md` §40.

| Attività | Stato | Dipendenze | Criterio di accettazione (sintesi) |
|---|---|---|---|
| M3bis.0 Inventario e mappa di adattamento | DONE | M3.3 | mappa in `WORKLOG.md`: bypass `DataTableRoot`/`detectFeaturesFromChildren` confermato, etichette da tradurre elencate, M3bis.5 fissato a 3 sessioni |
| M3bis.1 Righe annidate con subtotale ("Tree") | DONE | M3bis.0 | `getSottoRighe`/`CellaAlbero`/`meta.sottototale` in `data-table.tsx`; story `Albero` (computo a 2 livelli); tastiera verificata in Chromium reale (Enter/Spazio, cascata); a11y 288 scansioni/0 |
| M3bis.2 Espansione righe | DONE | M3bis.0 | `pannelloRiga`/`colonnaEspansione`/`getRowCanExpand` in `data-table.tsx`; story `Espansione` (righe escluse su `archiviato`); tastiera verificata in Chromium reale (Enter/Spazio); a11y 1156 scansioni/0 |
| M3bis.3 Resize e pin colonne generalizzato | DONE | M3bis.0 | D17 riaperta con conferma di Francesco; `ridimensionabile`/`colonneBloccabili` in `data-table.tsx`, `ui/table.tsx` **non toccato** (v. WORKLOG); story `Ridimensionabile`/`Colonne Bloccabili`; tastiera verificata (frecce, Home); a11y 1164 scansioni/0; `componenti-propri.json` resta vuoto |
| M3bis.4 Virtualizzazione / scroll infinito | DONE | M3bis.0 | `perPagina="virtuale"` (terza forma, non `"infinito"`); `@tanstack/react-virtual`; `DataTableBody`/`DataTableVirtualizedBody` estratti da `data-table.tsx`; story `Virtualizzata` (10.000 righe, mai più di ~36 `<tr>` montati); tastiera (frecce/Home/End/PageUp/PageDown) verificata in Chromium reale col fuoco che segue le righe montate/smontate; non compone con `pannelloRiga`; a11y 1168 scansioni/0 |
| M3bis.5 Data Grid editabile (**3 sessioni**: motore+clipboard/undo, celle+Zod, persistenza) | DONE | M3bis.3, M3bis.4 | Motore (`useDataGrid`) con fuoco per cella, clipboard, riempimento e annulla; sei celle tipizzate + `validaConZod`; `useGridChanges`. Story `Computo`, 500 righe. **Scarto**: le righe annidate non compongono con la Data Grid. a11y 1180/0. → `WORKLOG.md` M3bis.5 e coda |
| M3bis.6 Filtri (sfaccettato, intervallo, data, reset) | DONE | M3bis.0 | Quattro filtri (sfaccettato, intervallo, data, reset), su richiesta di Francesco. `barra` di `<DataTable>` passa anche `tabella`; le tendine restano sotto il grilletto. a11y 1188/0. → `WORKLOG.md` M3bis.6 e coda |
| M3bis.7 Drag&drop righe | DONE | M3bis.0 | `riordinabile={{ onRiordina }}` con `idRiga` **obbligatorio** (senza, dnd-kit anima un assestamento spurio); ordinamento, filtri e paginazione spenti mentre è attivo. Mouse e tastiera provati in Playwright. a11y 1192/0. → `WORKLOG.md` M3bis.7 |
| M3bis.8 Drag&drop colonne | DONE | M3bis.0 | `colonneRiordinabili` con maniglia separata dall'intestazione (niente `nested-interactive`); resize e riordino insieme. Coda: riordino da tastiera muto, corretto anche sulle righe di M3bis.7. a11y 1196/0. → `WORKLOG.md` M3bis.8 e coda |
| M3bis.9 Menu di riga condiviso, dropdown e tasto destro | DONE | M3bis.0 | `menuRiga`: lo stesso menu in tendina e al tasto destro, `enabledFor` spegne entrambi. Il tasto destro non compone con `perPagina="virtuale"` (scarto a verbale). Provato in Playwright. a11y 1200/0. → `WORKLOG.md` M3bis.9 |
| M3bis.10 Editing in-riga leggero (`chiaveMemoRiga`) | DONE | M3bis.0 | `chiaveMemoRiga`: si ridisegna solo la riga modificata (misurato); Invio salva, Esc annulla. Non compone con `perPagina="virtuale"`. **Rettifica**: `getRowMemoKey` non era nel core. Coda: 3 rilievi di Francesco. a11y 1204/0. → `WORKLOG.md` M3bis.10 e coda |
| M3bis.11a Aggiornamento `pagina-lista` (registry) | DONE | M3bis.1..M3bis.10 | `PaginaListaProps` inoltra `idRiga`, `ridimensionabile`, `colonneBloccabili`, `colonneRiordinabili`, `menuRiga`, applicati alla pagina vera. Coda: due difetti del riordino colonne e la maniglia di riga centrata. a11y 1204/0. → `WORKLOG.md` M3bis.11a e code |
| M3bis.11b Aggiornamento pagina Prodotti (Anagrafe, `stories/PaginaProdotti.stories.tsx`) e gate di fase | DONE | M3bis.11a | Resize, menu colonna, menu di riga (modifica, elimina con annullo) e filtri sfaccettati sulla pagina vera, provati col dito. a11y 1208/0. **FASE 3bis chiusa**, 14/14. Aperto: fondo di sorvolo translucido sulla colonna bloccata. → `WORKLOG.md` M3bis.11b e code |

## FASE 4 — Pagine modello (6 sessioni)

Gate: una app Vite vuota installa `tassullo-app-shell` + `pagina-lista` + `pagina-login` e in dieci minuti è navigabile e in stile, senza scrivere layout.
Dipendenze di fase: FASE 3. Ogni pagina è un `registry:block` con dati finti tipizzati.

| Attività | Stato | Dipendenze | Criterio di accettazione (sintesi) |
|---|---|---|---|
| M4.1 `pagina-login` | **DONE** 2026-09-15 | M3.4, M3.5 | ✔ **1 pagina, 72 item.** `PaginaLogin`: logo, nome app, `onAccedi` come sola cucitura verso MSAL/Entra ID (nessuna dipendenza `@azure/msal-react`). Tre stati (`inattivo`/`in-corso`/`errore`) più `configurato` ortogonale. `check:registry` esteso a `pages/` (nuova cartella, stessa regola-3-sola dei blocchi). `check` verde sui cinque gate, a11y 278 scansioni/0 violazioni. → dettaglio in `WORKLOG.md`, voce **M4.1** |
| M4.2 `pagina-lista` | **DONE** 2026-09-15 | M3.3, M3.5, M2.6 | ✔ **2 pagine, 74 item.** `PaginaLista`: `page-header` + `data-table` + gli stati, `stato` a tre vie, `vuotoIniziale` distinto dal vuoto per filtro. Provata in viewport × densità: **chiude D10**, verdetto (a). a11y 1128/0. → `WORKLOG.md` M4.2 |
| M4.3 `pagina-scheda` | **DONE** 2026-09-16 | M3.2, M3.4, M3.7 | ✔ **3 pagine, 75 item.** `PaginaScheda`: intestazione visibile, tab dentro `Card`, **un solo form sempre montato** con i campi `disabled` fuori modifica; `modifica` controllabile dall'app; `storico` monta `VersionTimeline`. a11y 1148/0. → `WORKLOG.md` M4.3 e coda |
| M4.4 `pagina-dashboard` | **DONE** 2026-09-18 | M2.8, M3.3 | ✔ **4 pagine, 80 item.** `PaginaDashboard`: avvisi, indicatori, due grafici, attività recenti sulla primitiva `table`. Zero componenti nuovi. Difetto muto corretto: `Card size="sm"` rendeva il numero a 13px invece di 27. a11y 1224/0. → `WORKLOG.md` M4.4 |
| M4.5 `pagina-admin` | **DONE** 2026-09-18 | M3.3, M3.4 | ✔ **5 pagine, 81 item.** `PaginaAdmin`: fascia a tab, banner fisso di sola lettura, e il **tab «Utenti» è del blocco** (tabella, ruoli multipli, rimozione con conferma), su indirizzo di Francesco. Ruoli come `string`, mai un'unione chiusa. a11y 1240/0. → `WORKLOG.md` M4.5 |
| M4.6 Stati di sistema e gate di fase | **DONE** 2026-09-18 | M4.1..M4.5 | ✔ **6 pagine, 82 item — FASE 4 chiusa.** `PaginaErrore` sulla primitiva `Empty`, quattro varianti. Tre difetti trovati dal gate e corretti: 26 `registryDependencies` rotte, due prerequisiti non documentati, `process.env` in quattro blocchi. Installazione ≈6m30s. a11y 1260/0. → `WORKLOG.md` M4.6 |
| M4.7 `item` (lista non tabellare) | **DONE** 2026-09-19 | M4.6 | ✔ **83 item.** Originale installato e snapshot preso prima di toccarlo; un solo ri-stile (`text-primary` → `text-accent-ink`). Il gate ha preso 8 `aria-required-children`, chiuse con `aria-hidden` sul separatore. a11y 1280/0. → `WORKLOG.md` M4.7 e coda |

## FASE 4ter — Copertura delle tre app (10 sessioni + 1 di coda)

**Gate superato il 2026-09-20, 10/10**, e revisione a video chiusa con sei sessioni di coda (`M4ter.11`–`M4ter.16`, 2026-09-21/22), tutte aperte da un rilievo di Francesco. Il Computo di Studio ha ora il suo blocco (`tassullo-foglio-gruppi`, M4ter.12): M5.5 lo nomina fra le pagine che possono migrare (`docs/ANALISI-COPERTURA-APP.md`).

Gate **superato in M4ter.10**: app Vite vuota, i **dodici** item nuovi installati e compilati in **36s di comandi**, e la tabella §1 di `docs/ANALISI-COPERTURA-APP.md` richiusa 12/12. Perché una fase e non una coda alla FASE 4: `PIANO.md` §FASE 4ter.
Dipendenze di fase: FASE 4. **Numeri di partenza**: `test:a11y` **1296 scansioni** (324 story × 4), 0 violazioni; `check:registry` 0 errori / 53 avvisi / **0 componenti nostri**; **83 item**. Ogni scena di story vale 4 scansioni. **Numeri d'arrivo (2026-09-20)**: `test:a11y` **1496** (374 story × 4), 0 violazioni; `check:registry` 0 errori / 62 avvisi / **1 componente nostro** (`entity-image`); **94 item**; bersagli **3246** con 0 piccoli.

| Attività | Stato | Dipendenze | Criterio di accettazione (sintesi) |
|---|---|---|---|
| M4ter.1 `@reui`: verifiche bloccanti, i tre file, gate a due provenienze | **DONE** | D21, D22 | **Fatto tutto**: stepper e le due viste del calendario, **10 file reui** verificati identici byte per byte. Gate a **provenienza per file**, con autotest. Trovato: `shadcn view` tronca a 64 KiB in silenzio, e `--snapshot` ora ripiega. **83 → 87 item**, 0 componenti nostri, a11y 1316/0. → `WORKLOG.md` M4ter.1 |
| M4ter.2 `tassullo-calendario` | **DONE** | M4ter.1 | **Fatto tutto**: una story sola, tre viste sullo stesso periodo, calendario = tipo di intervento, assegnatari = persone, dialogo crea/modifica/elimina. **87 → 89 item**, a11y 1332/0. Trovato un falso positivo del gate (import non normalizzati). → `WORKLOG.md` M4ter.2, `docs/DECISIONI.md` §44 |
| M4ter.3 `entity-image`, il primo componente nostro | **DONE** | D20 | **Fatto tutto**: prima riga di `componenti-propri.json` (0 → **1**), con autotest. Cella **256px**, misurata. Rapporti `4:3`, `16:9` e `1:1`. Due difetti muti: `alt=""` messo da Base UI e token di Tailwind emessi solo se usati. **89 → 90 item**, a11y 1356/0. → `WORKLOG.md` M4ter.3, `docs/DECISIONI.md` §45 |
| M4ter.4 `pagina-login` prende `modo` | **DONE** 2026-09-20 | D23 | Tre vie con **due interruttori** (`configurato`, `credenzialiAbilitate`) e `statoDi`. Non-regressione: 0 differenze di DOM oltre una virgola approvata. Firma a unione discriminata; logo Microsoft passato dall'app. a11y 1376/0. Code: il sesto gate `check:registry-build` e l'avviso SSO. → `WORKLOG.md` M4ter.4 e code |
| M4ter.5 Le cinque schermate d'accesso di Studio | **DONE** 2026-09-20 | M4ter.1, M4ter.4 | Sette scene in `Pagine/Schermate d'accesso`, **zero componenti nuovi** (90 item). Registrazione a `max-w-lg`, scelta di Francesco. Code: requisiti della password come controllo dinamico, sei regole e un misuratore di robustezza, che resta codice di pagina. a11y 1404/0. → `WORKLOG.md` M4ter.5 e code, `docs/SPEC-AUTH.md` |
| M4ter.6 `useSoglia` e la lista a due facce | **DONE** 2026-09-20 | M4ter.3 | **Il primo render è provato**: a 375px la `<table>` non compare mai. Le facce si scelgono con una prop della pagina; soglia di default **1024**, da decidere app per app. Tre difetti a video corretti. `check:registry` guarda anche `hooks/`. **90 → 91 item**, a11y 1416/0. → `WORKLOG.md` M4ter.6 |
| M4ter.7 `tassullo-indicatori` e le due prop di forma | **DONE** 2026-09-20 | M4.4, M4.3, M3.2 | Il blocco dichiara il **proprio** `@container/indicatori`: fuori dalla dashboard non resta più a due colonne. Una sorgente sola anche per lo scheletro. `titolo` → `ReactNode` e `descrizione` su `pagina-scheda`. **91 → 92 item**, a11y 1452/0. Coda: quattro difetti a video. → `WORKLOG.md` M4ter.7 |
| M4ter.8 Piede di tabella, conferma digitata, chip col conteggio | **DONE** 2026-09-20 | M3.3, M3.5 | ✔ **a11y 1472/0, sei gate verdi.** `<tfoot>` scritto una volta, che **segue il filtro**; conferma digitata con `onConferma(valore)`. Code: separatore delle migliaia sempre (`docs/DECISIONI.md` §47, nuovo item `numeri`, 92 → 93) e `font-mono` sospeso sui codici (§48). → `WORKLOG.md` M4ter.8 e code |
| M4ter.9 `tassullo-barra-contesto` | **DONE** 2026-09-20 | M4.7 | ✔ **94 item, a11y 1496/0**, sei gate verdi. Composizione pura, nessuna variante nuova. `Esc` e clic fuori riportano il fuoco al grilletto. Tre difetti trovati guardando. Coda: fascia più visibile (bordo). Slot in `app-shell` non aggiunto: innesco non scattato. → `WORKLOG.md` M4ter.9, `docs/DECISIONI.md` §42 |
| M4ter.10 Gate di fase | **DONE** 2026-09-20 | M4ter.1..M4ter.9 | ✔ **FASE 4ter chiusa, 10/10, 94 item.** Installazione in un'app vuota in **36s** di comandi. Quattro difetti che nessun gate vedeva, fra cui un riferimento rotto e la palette del preset che copriva il tema. Nuovo **settimo gate** `check:riferimenti`. `weekend` del calendario a `false`. → `WORKLOG.md` M4ter.10 |
| M4ter.11 Revisione a video: tutto quel che resta | **DONE** 2026-09-21 | M4ter.10 | ✔ Cinque difetti corretti e rimisurati; sette scelte di forma chiuse a video (fra cui via l'intestazione di `pagina-scheda` e lo slot `AppShell.contesto`). **D24**: tavolozza `--chart-1..10` (`docs/DECISIONI.md` §49). Triage e SistemaEditor si ricompongono, il Computo no (→ M4ter.12). a11y 1496/0. → `WORKLOG.md` M4ter.11 |
| M4ter.12 Il Computo di Studio: fuoco di cella su un albero | **DONE** 2026-09-21 | M4ter.11 | **`tassullo-foglio-gruppi` scritto**, gradino 4 approvato da Francesco: le frecce attraversano zone e gruppi, **Tab da 24 fermate a 1**. Quattro rilievi a video chiusi. **D25**: `input`/`textarea` a 16px sotto `md`, contro lo zoom di iOS. a11y 1520/0, 95 item. → `WORKLOG.md` M4ter.12, `docs/DECISIONI.md` §50–51 |
| M4ter.13 Il picker d'inserimento voci di Studio | **DONE** 2026-09-22 | M4ter.12 | **Si compone: nessun componente nuovo.** `responsive-dialog` + `command` + `toggle-group` + `badge`, ricetta in `Pagine/Scelta da catalogo`. Tre difetti presi misurando. Aperto: `accent-ink` su `--accent` dà **4,29:1**, coppia che `check:contrast` non copre. a11y 1524/0. → `WORKLOG.md` M4ter.13 |
| M4ter.14 La testata di `Pagine/Lista` allineata a Prodotti | **DONE** 2026-09-22 | M4ter.13 | `IntestazioneColonnaMenu` promossa nel blocco `data-table`, opt-in. Lista allineata a Prodotti: un solo grilletto per colonna, filtri sfaccettati su Categoria e Stato. Coda: chiuso il riquadro che filtrando non ricresceva più. a11y 1524/0. → `WORKLOG.md` M4ter.14 e coda |
| M4ter.15 La faccia stretta di `Pagine/Prodotti` | **DONE** 2026-09-22 — validata da Francesco | M4ter.14 | **Schede**, non la forma D: un catalogo si sfoglia. `useSoglia` a **1024px**; sotto soglia Stato a chip e Famiglia come raggruppamento, nessun popover. Cinque difetti a video, fra cui il fianco destro della `Card` tagliato. Indice Storybook alfabetico. a11y 1532/0. → `WORKLOG.md` M4ter.15 |
| M4ter.16 La faccia stretta di `Pagine/Lista` (Norme) | **DONE** 2026-09-22 | M4ter.15 | **Il bivio sta nel blocco**: `tassullo-pagina-lista` prende `facciaStretta`, `soglia` (1024px) e `faccia`; il blocco sceglie *quando*, la pagina scrive *cosa*. Raggruppa per Categoria, Stato a chip. `@tassullo/use-soglia` dichiarato. a11y 1540/0. → `WORKLOG.md` M4ter.16 |

## FASE 5 — Registry, distribuzione e guida di adozione (11 sessioni)

Gate: l'installazione in un'app vergine produce un'app in stile Tassullo che compila — **verificata in locale, prima di qualunque pubblicazione** — seguendo solo i documenti scritti qui.
Dipendenze di fase: FASE 4.
**M5.0 aggiunta in testa il 2026-09-22**: le note interne comparivano nel testo visibile della style guide in quasi tutti i file. Cinque sessioni, un canone di pagina, un ottavo gate `check:storybook`. → `PIANO.md` §M5.0

| Attività | Stato | Dipendenze | Criterio di accettazione (sintesi) |
|---|---|---|---|
| M5.0a Canone, `Introduzione`, pagine `Tema/*`, gate in modalità avviso | **DONE** 2026-09-22 | M4ter.16 | `check:storybook` sull'AST di TypeScript, con autotest. Conto di partenza **568 su 85 file**, dopo le sette pagine **514**. `autodocs` acceso per tutti: 90 pagine Docs. a11y 1540/0. → `WORKLOG.md` M5.0a |
| M5.0b Primitive, prima metà (`accordion`…`item`, **28** file) | **DONE** 2026-09-22 | M5.0a | 28 pagine riscritte col canone: **152 → 0**, repo **514 → 362**. Coda: le pagine Docs seguono la Modalità; contrasto delle Docs in scuro **47 → 0**. a11y 1540/0. → `WORKLOG.md` M5.0b e coda, `docs/DECISIONI.md` §52–53 |
| M5.0c Primitive, seconda metà (`kbd`…`tooltip`, **24** file) | **DONE** 2026-09-23 | M5.0b | 24 pagine riscritte: **123 → 0**, `ui/` a 0, repo **362 → 239**. Tre affermazioni di tastiera corrette (§54); lo stepper resta identico a reui (§55). a11y 1540/0. → `WORKLOG.md` M5.0c e coda |
| M5.0d Blocchi (21 file) | **DONE** 2026-09-23 | M5.0c | 21 pagine riscritte: **129 → 0**, repo **239 → 110**. Coda su `data-grid` e foglio a gruppi: `Tab` esce dalla griglia, numeri con `numeri`, prove in Safari, maniglie di ridimensionamento fuori dal `Tab`. a11y 1540/0. Aperti: i JSDoc dei sorgenti nella tabella delle prop (→ M5.0e). → `WORKLOG.md` M5.0d, `docs/DECISIONI.md` §56 |
| M5.0e Pagine (6 + 4), nomi fittizi, gate armato in `check` e in CI | **DONE** 2026-09-23 | M5.0d | Pagine **110 → 0**; il gate legge anche tabella delle prop e «Show code»: da **204** a 0. Nomi inventati, `esempio.it`. `check` verde, a11y **1544/0** (386 story); Docs pulite in Chiaro e Scuro; `grep` su `storybook-static/` 0 file fuori da `tema/` ed `esempi/`; Pages riletto dall'URL pubblico. → `WORKLOG.md` M5.0e, `docs/DECISIONI.md` §57 |
| M5.1a Gate dipendenze e testo spedito | DONE | M5.0e | `check:riferimenti` guarda anche i **510 import** contro la chiusura: 2 item che nell'app non compilavano, corretti. **`check:spedito`**, decimo gate: 90 note nei campi a 0, `ui/` `lib/` `hooks/` e tema puliti. `shadcn add` toglie le teste dei file (§58): le istruzioni del tema spostate dove arrivano. Restano 297 note in 18 file, a tetto |
| M5.1b Testo spedito, famiglia tabella | DONE | M5.1a | 193 note a 0 nei 6 file, righe tolte da `DA_RIPULIRE`. Il gate non leggeva i commenti JSX `{/* */}`: corretto, +24 note. `docs` completato per i 6 item; tolta la promessa falsa delle colonne `ancorata`, corretto il JSDoc di `tabella` dei filtri. Il filtro data non si cancellava da tastiera: aggiunto «Cancella», scena nuova nel gate |
| M5.1c Testo spedito, altri blocchi e pagine | DONE | M5.1b | 117 note a 0 in 12 file; `DA_RIPULIRE` tolto dallo script: da qui nessuna esenzione. `docs` verificato per i 12 item: cinque dicevano cose non più vere (limite di larghezza del guscio, slot del contesto «assente», tabella della dashboard, cinque colori del calendario), corretti |
| M5.2 Prova d'installazione end-to-end | **DONE** 2026-09-24 | M5.1c | Fusa con M5.3: tre giri da cartella vuota, da GitHub (`main`), seguendo solo `docs/INTEGRAZIONE.md`. Giro 1 con **9 attriti**, giro 2 con uno nuovo, giro 3 senza deviazioni: **58 s** di comandi. Pagina lista nel guscio in chiaro, scuro e touch, in Chromium; schermate in `docs/img/M5.2/`. → `WORKLOG.md` M5.2+M5.3 |
| M5.3 `INTEGRAZIONE.md` v2 (app nuova) | **DONE** 2026-09-24 | M5.2 | `docs/INTEGRAZIONE.md` scritto: contesto, nove passi cronometrati, pagine modello col comando, densità e scuro, identità diversa (provata), aggiornare. I prerequisiti del `README` passano qui. Corretto il `docs` di `tema`: da togliere sono quattro cose, non due. → `docs/DECISIONI.md` §60 |
| M5.4 Regole per `CLAUDE.md` e piani delle app | DONE | M5.3 | blocco in `docs/INTEGRAZIONE.md`, voce di roadmap e paragrafo di checklist, incollati tal quali in un'app nuova: 3 attriti dalla prova, 37 da tre riletture di agenti senza contesto; le due domande aperte decise in §62 (codici in tabella) e §63 (canale delle proposte). `DECISIONI.md` §61 (no npm, Base UI, `primary`≠`accent`, oklch, Inter in data URI). Dettaglio in `WORKLOG.md`. |
| M5.5a Anagrafe letta, e la prova di convivenza | DONE 2026-09-24 | M5.4, **FASE 4ter** | Clone senza remoto. 19 pagine → blocco v2, 3 lacune candidate non aperte (`ANALISI-COPERTURA-APP.md` §9). Convivenza: 20 nomi in comune col tema di Tailwind, l'azzeramento sposta 976 elementi v1 su 1.454. **Strada D** (Francesco): v2 20/20, pagine v1 solo Inter e +1px → `DECISIONI.md` §64 |
| M5.5b `GUIDA-MIGRAZIONE.md` e passo 0 | TODO | M5.5a | la guida per il passaggio **in un colpo solo**: v1 e v2 non convivono, tutto su un ramo, via il v1 nello stesso lavoro (`DECISIONI.md` §64, «Seguito»). Più la transizione di `CLAUDE.md`/`INTERFACCE.md` di Anagrafe; **passo 0 provato nel clone**; le modifiche ai `.md` preparate in `transizione-anagrafe/`, non caricate. Raccoglie le «Note per M5.5» di `ANALISI-COPERTURA-APP.md`. |
| M5.6 Pubblicazione su GitHub + Pages | IN_PROGRESS | M5.5 | **Repo online e Pages attivo dal 2026-09-10** (D4 chiusa e anticipata). Restano: tag `v2.0.0` e install da GitHub riuscita fuori dal repo. La storia è **già stata ripulita** il 2026-09-09 (i font in base64 tolti da tutti i commit con `git-filter-repo`) → dettaglio in `WORKLOG.md` |

## Decisioni

| Decisione | Stato | Blocca | Nota |
|---|---|---|---|
| D2 valori della palette scura | **CHIUSA** 2026-09-07 | — | chiusa guardandola affiancata al chiaro, non al buio. Neutri dalla sidebar v1, brand invariato, tenui specchiati a gradini fissi; tre scostamenti misurati (`--sidebar`, `--info`, `--warning`). Motivazione in `WORKLOG.md` e `PIANO.md` §2bis |
| D3 come si servono i font alle app | **DONE** (2026-09-08) | nessuno | **Chiusa**: Inter va nel registry, item `tema-font`, coi file in **data URI** dentro il CSS — perché `shadcn build` legge i file come testo e un `.woff2` arriverebbe corrotto senza errore (73.016 byte → 69.186 caratteri, misurato). → dettaglio in `docs/DECISIONI.md` e in `WORKLOG.md` |
| D4 quando pubblicare su GitHub | **CHIUSA** 2026-09-10 — **subito, pubblico** | — | Forzata più che scelta: org su piano **free**, dove Pages vive solo sui repo pubblici, e la scorciatoia `owner/repo/item` della CLI shadcn è documentata solo per il pubblico. Privato avrebbe messo un token GitHub in ogni app, CI ed editor. Tolto dal repo `public/fonts/` per intero (nota Lineto). → `WORKLOG.md`, voce **D4** |
| **D20** il primo componente nostro: l'immagine d'entità con segnaposto | **CHIUSA** 2026-09-19 — **sì, come componente** | **M4ter.3** | Prima riga di `componenti-propri.json`. `avatar`, `ItemMedia` e `aspect-ratio` non bastano; scartate la sovrascrittura dal punto di chiamata e la variante in `avatar.tsx` (vietata da 4bis). Scritta in M4ter.3. → `docs/DECISIONI.md` §45 |
| **D21** il registry `@reui` si dichiara in `components.json`? | **CHIUSA** 2026-09-19 — **sì**, dichiarato | **M4ter.1**, e da lì M4ter.2 e M4ter.5 | Una riga additiva: l'MCP legge solo i registry dichiarati, ed è per questo che il calendario risultava introvabile. `shadcn view @reui/…` risponde. → `docs/ANALISI-COPERTURA-APP.md` §3 e §6.11 |
| **D22** il calendario a eventi: si scrive o si adotta? | **CHIUSA** 2026-09-19 — **si adotta `@reui/event-calendar`** | **M4ter.1**; si riapre se una delle tre verifiche non regge | Reui è Base UI con il nostro stesso `style`, e porta le barre multi-giorno. Il componente è MIT in `keenthemes/reui` (avviso di copyright da conservare); i Pro Blocks no. → `docs/ANALISI-COPERTURA-APP.md` §6.11 |
| **D23** le vie d'accesso di `pagina-login` | **CHIUSA** 2026-09-19 — **`modo: "microsoft" \| "credenziali" \| "entrambi"`**, default `microsoft` | **M4ter.4**, e M4ter.5 per le altre schermate | Studio ha utenti fuori dall'azienda: l'SSO puro è impossibile. Insieme chiuso perché su Studio l'SSO è il caso secondario; il default tiene Anagrafe e Officina immutate. → `docs/SPEC-AUTH.md` |
| D19 `native-select` di shadcn entra nel registry? | **CHIUSA** 2026-09-19 — **no** | gli 81 `<select>` a mano passano al `select` in **M5.5** | La tendina nativa la disegna il sistema operativo e si comporta diversamente su Windows e su macOS: l'opposto di un design system. Il `select` Base UI salta alla voce premendo una lettera, verificato. → `docs/DECISIONI.md` §42 |
| D18 conferma o annullo, per quali azioni | **CHIUSA** 2026-09-15 — **differita lato client, provvisorio** | M3.5 (`DONE`) | Annullo **differito lato client**, senza soft-delete al backend; da rivedere con Roberto a design system finito. Costruito come `tassullo-toast-con-annullo` in M3.5. → `docs/DECISIONI.md` §35 |
| D17 resize delle colonne da parte dell'utente | **CHIUSA** 2026-09-11 — **no per ora**, col grilletto scritto | riapre alla prima tabella in cui dichiarare le larghezze non basta | `meta.larghezza` risolve già la colonna troppo stretta, a costo zero, e la maniglia sarebbe un componente nostro. Nulla la preclude: `table-fixed` è un prerequisito del resize. → `docs/DECISIONI.md` §34, coi quattro costi per chi riaprirà |
| D5 destino di `components.css` e `stylelint-config.cjs` v1 | TODO | nessuno | fuori dal perimetro: si dismettono a ultima app migrata |
| D6 quando migrano Anagrafe / Studio / Officina | TODO | nessuno | ognuna decide per sé con la guida M5.5; nessuna scadenza |
| D7 se `docx/` resti nel repo v1 | TODO | nessuno | nessuna urgenza, mai |
| D8 librerie di terze parti per i blocchi | **DONE** 2026-09-15 | M3.6..M3.9 | Le quattro: `react-dropzone` (M3.6), `react-pdf` (M3.7), `@tiptap/react` (M3.8), `diff` (M3.9) — tutte hook/funzione pura, nessun markup di libreria da disfare, sostituibili senza toccare l'API del blocco |
| D12 `next-themes` va tolto da `sonner`? | **CHIUSA** 2026-09-09 — **no, si lascia** | nessuno | Aperta e chiusa in giornata, in M2.3. `sonner.tsx` importa `useTheme` da `next-themes`, che non usiamo: la modalità la mettiamo come classe sulla radice. → dettaglio in `docs/DECISIONI.md` e in `WORKLOG.md` |

| D13 il marchio Tassullo come item del registry | **CHIUSA** 2026-09-10 — **(c) `mask-image` nel CSS del tema** | **entro la fine della FASE 2** (M2.9) | Aperta in M2.5. **L'asset non manca più**: Roberto ha segnalato che Anagrafe ha il marchio in produzione da tempo — `frontend/public/tassullo-t.svg`, un `path` solo con due sottotracciati, `viewBox` 24×38, `fill-rule="evenodd"`. → dettaglio in `docs/DECISIONI.md` e in `WORKLOG.md` |
| D16 la scala tipografica è due punti sotto shadcn | **CHIUSA E ATTUATA** 2026-09-10 — **«+1» su ogni gradino, `md` fuso in `sm`, `title` eliminato** | attuata in **M1.6** (`DONE`) | Aperta in M3.1 da Francesco, confrontando la style guide coi blocchi di `ui.shadcn.com`. → dettaglio in `docs/DECISIONI.md` e in `WORKLOG.md` |
| D14 i bottoni-icona del combobox senza nome accessibile | **CHIUSA** 2026-09-09 — **si accettano, non si diverge** | nessuno | Aperta e chiusa in giornata, in M2.6. Il chevron che apre l'elenco e la crocetta di ogni pillola portano solo l'icona: axe dà `button-name`, gravità *critical*, 16 violazioni. → dettaglio in `docs/DECISIONI.md` e in `WORKLOG.md` |
| D11 il testo lungo: si adotta `typeset` di shadcn? | **CHIUSA** 2026-09-15 — **no** | attuata in **M3.8** (`DONE`) | Il builder non è uno script, e lo schema di `rich-text-editor` è troppo ridotto per servirsene. Si riapre se un consumatore di testo lungo vero lo chiede. → `docs/DECISIONI.md` §36 |
| D10 la densità touch su schermo stretto | **CHIUSA** 2026-09-15 — **(a) si tiene così com'è** | attuata in **M4.2** (`DONE`) | Sulla pagina più densa, a 375px × touch, restano **327px** utili come in M3.1, nessun bersaglio sotto soglia. Scartate le uscite (b) e (c). → `WORKLOG.md` M4.2, `docs/DECISIONI.md` §29 |
| D15 la tastiera del calendario non naviga | **CHIUSA** 2026-09-09 — **si accetta, non si diverge** | nessuno | Aperta e chiusa in giornata, in M2.7. → dettaglio in `docs/DECISIONI.md` e in `WORKLOG.md` |
| D16 quale set d'icone | **CHIUSA** 2026-09-10 — **si tiene Lucide** | nessuno | Scelta di Francesco fra i cinque set che la CLI shadcn sa installare (Lucide, Tabler, Phosphor, Remix Icon, HugeIcons), confrontati sulle 39 icone che le app useranno davvero. → dettaglio in `docs/DECISIONI.md` e in `WORKLOG.md` |

Accertamenti tecnici già chiusi sul campo (con la prova): **`docs/DECISIONI.md`**, nato in M0.2. Dove contraddice `PIANO.md`, vince lui e la contraddizione è annotata.

Chiuse il 2026-09-07 (registro in `PIANO.md` §4): **D1** — nome `tassullo-design-system-v2`; **D9** — libreria di primitive **Base UI**, con unica eccezione possibile sul calendario (M2.7), da motivare in WORKLOG.

## Candidati sospesi

Cose che **non** sono a piano, scritte qui con l'**innesco** che le riapre. Non è un elenco di desideri: un candidato non scritto è un candidato che si riscopre da zero, ed è già successo con `native-select`, analizzato due volte prima di diventare D19.

| Candidato | Stato | Innesco che lo riapre | Nota |
|---|---|---|---|
| **Il mese del calendario si schiaccia sotto gli ~880px di contenitore** | **CHIUSO in M4ter.11** (2026-09-21): il mese scorre davvero | già scattato: Calendario di Officina su un portatile 13" | Sotto ~880px di contenitore le righe del mese si sovrapponevano (50px a 576) e un evento del 21 sembrava del 14. → `WORKLOG.md` coda di M4ter.10 e M4ter.11 |
| **Una tavolozza estesa (10–15 colori) per grafici, calendario e categorie** | **CHIUSA in M4ter.11** (2026-09-21), D24 → `docs/DECISIONI.md` §49 | — | `--chart-1..10`, una famiglia sola; le tinte di categoria vogliono 3:1, con arancio e verde del brand esentati per indice. Si perde il bianco e nero: chi stampa in B/N usa `--chart-mono-1..5`. `--chart-10` è un rosso caldo, non `--destructive`. |
| **Lo slot in `app-shell`** per una fascia persistente fra la testata e `<Outlet/>` | SOSPESO 2026-09-19, innesco non scattato (ricontrollato in M4ter.11) | **quando una seconda app ha un contesto attivo che attraversa le pagine** (un impianto, una famiglia, un cantiere) | Oggi ce l'ha solo Studio, e la pagina monta la barra da sé. Da non confondere con `AppShell.contesto` (M4ter.11), che sta nella testata della colonna: questo starebbe sopra il contenuto. → `docs/ANALISI-COPERTURA-APP.md` §1 |

## Assunzioni sotto osservazione

| Assunzione | Nota |
|---|---|
| A1 le app consumer restano su Vite + React + TypeScript | se cambia, cambia il target dei `registry:block` |
| A2 Tailwind v4 accettabile in tutte le app | introduce una build-dependency che il v1 non aveva |
| A3 taratura delle sessioni per analogia con Anagrafe | si verifica sul consuntivo, fase per fase |
| A4 esisterà una **app nuova** su cui il v2 debutta | se tarda troppo, anticipare una pagina di Anagrafe come collaudo (riapre D6) |
