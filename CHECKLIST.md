# CHECKLIST — **Tassullo Design System 2.0**

> **Fonte di verità dell'avanzamento.** Stati: `TODO | IN_PROGRESS | BLOCKED | REVIEW | DONE`. Si aggiorna a ogni cambio di stato e al termine di ogni attività significativa. Il dettaglio dei task (Prompt, File, Accettazione completa) è in `PIANO.md`, che qui non si duplica; il diario è `WORKLOG.md`.
> Un task = una sessione Claude Code, salvo indicazione contraria. **Totale al 2026-09-19: 64 righe = 67 sessioni.** Nel piano originale erano **41 righe = 42 sessioni**: `M3.3 data-table` vale 2 sessioni (unico task doppio del piano originale). **FASE 3bis** (aggiunta il 2026-09-16, niko-table) porta altre 12 righe = 14 sessioni: `M3bis.5 data-grid` vale 3 sessioni (fissate in M3bis.0), `M3bis.9` aggiunta il 2026-09-16 su richiesta di Francesco (menu di riga condiviso), `M3bis.11` divisa il 2026-09-17 in `11a` (`pagina-lista`) e `11b` (pagina Prodotti di Anagrafe) su richiesta di Francesco. **M4.7** aggiunta in coda alla FASE 4 il 2026-09-18. **FASE 4ter** (aggiunta il 2026-09-19, gap analysis delle tre app) porta 10 righe = 10 sessioni.
> **La colonna del criterio è una sintesi, e ha un tetto.** Il verdetto, i due o tre numeri che contano, un rimando: **due o tre righe di tabella, non di più**. Il ragionamento, le misure, le trappole e le rettifiche stanno in `WORKLOG.md` e in `docs/DECISIONI.md` — qui non si duplicano. Non è pignoleria: una riga che non si legge smette di essere una fonte di verità, ed è quello che era successo. Ripulita il 2026-09-10 su rilievo di Francesco — **da 48 a 26 KB**, la riga peggiore da 3260 a 426 caratteri — senza perdere niente, perché ogni task ha la sua voce nel diario e ogni decisione la sua sezione.
> Nessuna colonna Responsabile: il progetto è condotto da una sola persona (Francesco) con Claude Code. Se un giorno lavorassero in due, si aggiunge qui la colonna e si adotta la regola dei worktree di Anagrafe.

## FASE 0 — Scaffold e fondamenta (5 sessioni)

Gate: `npm run storybook` mostra una primitiva shadcn di prova con Tailwind v4 attivo e i tre interruttori (tema, densità, viewport); i quattro documenti di conduzione esistono; git locale inizializzato. **Gate superato il 2026-09-07** (commit `a435201`).

| Attività | Stato | Dipendenze | Criterio di accettazione (sintesi) |
|---|---|---|---|
| M0.1 Documenti di conduzione e struttura | DONE | | `CHECKLIST.md`, `WORKLOG.md`, `CLAUDE.md` coerenti con `PIANO.md`; tutti i task elencati; `CLAUDE.md` con tutte le regole del piano |
| M0.2 Scaffold Vite + Tailwind v4 + `shadcn init` | DONE | M0.1 | `npm run dev` con utility Tailwind attiva; `shadcn add button` compila **in variante Base UI**, meccanismo di selezione scritto in `docs/DECISIONI.md` |
| M0.3 Storybook: la style guide 2.0 | DONE | M0.2 | `npm run storybook` apre l'indice con una story di prova; i tre interruttori funzionano; `build-storybook` produce `storybook-static/` servibile |
| M0.4 MCP shadcn e prerequisiti d'ambiente | DONE | M0.2 | dall'MCP si elencano i componenti shadcn **e** gli item del registry Tassullo locale; versioni Node/npm annotate in `docs/DECISIONI.md` |
| M0.5 Git locale | DONE | M0.1..M0.4 | `git log` col commit iniziale, `git remote -v` vuoto (D4), `CLAUDE.md` rivisto su ciò che le 4 sessioni hanno prodotto davvero |

## FASE 1 — Tema Tassullo (5 sessioni)

Gate: la pagina Palette mostra tutte le coppie token nelle 4 combinazioni (light/dark × normale/touch); `check:contrast` passa su tutte le coppie `X`/`X-foreground`.
**Superata** con M1.3–M1.5: `check:contrast` verde su **48 coppie**, le story `Tema/Palette` (56 token, valore letto dal DOM) e `Tema/Densità`, e il tema che esce dal repo come item con **installazione provata end-to-end** su un'app vuota. Riaperta e richiusa da **M1.6**, che ha riallineato la scala tipografica → `WORKLOG.md`, voci M1.3–M1.6.
Dipendenze di fase: FASE 0.

| Attività | Stato | Dipendenze | Criterio di accettazione (sintesi) |
|---|---|---|---|
| M1.1 Mappa dei token e script di conversione | DONE | M0.2 | `PIANO.md` §2bis scritto; `npm run check:contrast` gira e **fallisce** se si forza `--primary-foreground` a bianco |
| M1.2 `tassullo-theme.css`, modalità chiara | DONE | M1.1 | contrasto verde (24/24); `bg-primary text-primary-foreground` rende arancione con testo nero, verificato a video |
| M1.3 Modalità scura (chiude D2) | DONE | M1.2, M0.3 | contrasto verde 48/48 (24 per modalità); story `Tema/Palette` con le due palette affiancate e `docs/img/M1.3-palette-chiaro-scuro.png`; **D2 chiusa** |
| M1.4 Densità touch | DONE | M1.2, M0.3 | interruttore verificato senza ricaricare; default **48px** (xs 36, sm 42, lg 54, icona 24), misure lette dal DOM nella story `Tema/Densità`; due eccezioni annotate (sidebar → M2.5, `text-[0.8rem]` di `sm` → M2.1) |
| M1.5 Il tema come item di registry | DONE | M1.3, M1.4 | `registry validate` verde su 2 item; **prova d'installazione end-to-end** su app Vite vuota, con `vite build` che compila `text-*` come `var()` e i due blocchi densità; pagina Palette coi 56 token, valore letto dal DOM e click-to-copy; `cssVars`/`css` scartati con la prova (`DECISIONI.md` §11) |
| M1.6 Riallineamento della scala tipografica | **DONE** 2026-09-10 | D16 chiusa | ✔ attuata **D16**: scala in vigore `xs 12 / sm 13 / base 15 / lg 16 / xl 19 / 2xl 27 / 3xl 31`, **zero gradini nostri** — `--text-md` fuso in `sm`, `--text-title` diventato `2xl`, `3xl` tarato accanto. → dettaglio in `WORKLOG.md`, voce **M1.6** |

## FASE 2 — Primitive (9 sessioni)

Gate: M2.9 verde su tutto il set (contrasto, tastiera, densità, dark), con axe-core in CI. **Superato il 2026-09-10**: `npm run test:a11y`, **852 scansioni, 0 violazioni**, dentro `npm run check` e in `.github/workflows/gate.yml`. Dipendenze di fase: FASE 1; ogni primitiva in variante **Base UI** (D9).

**Due rilievi restano aperti.** Gli altri tre sono evasi da M2.9, e la loro storia — coi conti che cambiavano quando la misura imparava ad aprire i popup, e le due rettifiche di falsi positivi — sta in `WORKLOG.md` (M2.6, M2.9) e in `docs/DECISIONI.md` §27.

1. **Il contorno dei controlli sta sotto 3:1** (WCAG 1.4.11), su tutto il set e in entrambe le modalità: `border-input` misurato **1.27 in chiaro, 1.36 in scuro**. Non viene dal ri-stile, è il valore ereditato dal v1, e `check:contrast` non lo vede perché verifica **solo coppie di testo**. È una decisione di **palette**, in `hex-to-oklch.ts`.
2. **Filtri: sorvolato e acceso sono lo stesso grigio** — noto e **accettato** (Francesco, 2026-09-09): tingere ogni filtro acceso col brand gli toglierebbe il potere di segnalare. **Si riprende in FASE 4**, su una barra filtri vera.

| Attività | Stato | Dipendenze | Criterio di accettazione (sintesi) |
|---|---|---|---|
| M2.1 Fondamenta (`button`, `badge`, `separator`, `skeleton`, `spinner`, `avatar`, `kbd`, `button-group`, `typography`, `lib/utils.ts`) | DONE | M1.5 | `button` in tutte le varianti/stati nelle 4 combinazioni; `typeset` riproduce la scala v1 in **Inter** e fissa la regola delle cifre; **chiusi i 4 rilievi su `button`** più l'*incomplete* su `ghost` e il `text-[0.8rem]` di `sm` → dettaglio in `WORKLOG.md`, voce **M2.1** |
| M2.2 Form (`field`, `input`, `input-group`, `label`, `textarea`, `select`, `checkbox`, `switch`, `radio-group`, `slider`) | DONE | M2.1 | form di prova con **14 fermi di tabulazione**, tutti in ordine di DOM, tutti con nome accessibile e anello di fuoco visibile; **axe 158 scansioni (79 story × 2 modalità), 0 violazioni**. → dettaglio in `WORKLOG.md`, voce **M2.2** |
| M2.3 Overlay (`dialog`, `alert-dialog`, `drawer`, `sheet`, menu, `popover`, `tooltip`, `sonner`, `command`) | DONE | M2.1 | **11 primitive, 32 item, `registry validate` verde.** Fuoco intrappolato ed `Esc` verificati **in un browser vero** su tutti e 5 i modali: `Invio` apre, 8 `Tab` non escono, `Esc` chiude, il fuoco torna al grilletto (il `popover` non intrappola, ed è voluto: non è modale). → dettaglio in `WORKLOG.md`, voce **M2.3** |
| M2.4 Contenuto (`card`, `tabs`, `table`, `alert`, `empty`, `accordion`, `collapsible`, `scroll-area`, `resizable`, `progress`, `aspect-ratio`, `carousel`) | DONE | M2.1 | **12 primitive, 44 item, `registry validate` verde.** Le quattro varianti semantiche di `alert` si fanno con `className`, non con quattro nomi di variante: scala 4bis ferma al gradino 1, e la strada opposta è stata provata e misurata → dettaglio in `WORKLOG.md`, voce **M2.4**, e `docs/DECISIONI.md` §25 |
| M2.5 Navigazione (`sidebar`, `breadcrumb`, `pagination`) | DONE | M2.1 | **3 primitive + 1 hook, 48 item, `registry validate` verde.** Sidebar antracite già dai token `--sidebar-*`, e lo stato attivo del preset **coincide alla lettera** con la regola del v1 (`bg-sidebar-accent` + `sidebar-accent-foreground`): scala 4bis ferma al gradino 1 su tutte e tre. → dettaglio in `WORKLOG.md`, voce **M2.5** |
| M2.6 Filtri e selezione (`toggle`, `toggle-group`, `combobox`, `multi-select`/`tag-input`) | DONE | M2.2, M2.3 | **3 primitive, 51 item, `registry validate` verde.** Il piano ne chiedeva quattro: `combobox` **non si compone più** da `command` + `popover`, è un item shadcn suo su Base UI, e con `multiple` porta **anche** il multi-select e il tag-input — pillole comprese. → dettaglio in `WORKLOG.md`, voce **M2.6** |
| M2.7 Date (`calendar`, `date-picker`) | DONE | M2.3 | **1 primitiva, 52 item, `registry validate` verde.** **L'eccezione a Base UI di D9 non si pone**: Base UI non ha un calendario e shadcn ne spedisce uno solo, su `react-day-picker` + `date-fns` — nessun secondo candidato da valutare, quindi niente da motivare. → dettaglio in `WORKLOG.md`, voce **M2.7** |
| M2.8 Dati (`chart`, palette categorica) | DONE | M2.4 | **1 primitiva, 53 item, `registry validate` verde.** `--chart-1..5` non sono cinque colori scelti ma **cinque pioli di una scala di chiarezza**, e il passo non è deciso: è **1.4935:1**, quello che `--primary` e `--success` hanno già fra loro (misurato). → dettaglio in `WORKLOG.md`, voce **M2.8** |
| M2.9 Gate di fase: audit del set | DONE | M2.1..M2.8 | **852 scansioni, 0 violazioni.** `a11y.test = 'error'`; `addon-vitest` + Playwright/Chromium; `npm run test:a11y` fa **quattro passate** — {chiaro, scuro} × {popup chiuso, aperto} — e **dichiara quali popup apre** (47/48). → dettaglio in `WORKLOG.md`, voce **M2.9** |

## FASE 3 — Blocchi applicativi Tassullo (11 sessioni)

Gate: M3.10 — una pagina reale di Anagrafe ricostruita in Storybook **usando solo i blocchi**, senza CSS di pagina.
Dipendenze di fase: FASE 2. Ogni blocco dichiara i propri `registryDependencies`.

| Attività | Stato | Dipendenze | Criterio di accettazione (sintesi) |
|---|---|---|---|
| M3.1 `tassullo-app-shell` | **DONE** 2026-09-10 | M2.5 | ✔ quattro celle viewport × densità misurate in Chromium: larghezza utile **1148 / 1008 / 343 / 327px**, caratteri per riga **165 / 136 / 49 / 44**; prima misura di D10 a verbale (`docs/DECISIONI.md` §29). → dettaglio in `WORKLOG.md`, voce **M3.1** |
| M3.2 `page-header` | **DONE** 2026-09-10 | M3.1, M2.5 | ✔ **2 blocchi, 56 item, `registry validate` verde.** La pagina *dichiara* percorso e azioni, un **portale** le rende nella fascia: il guscio si monta una volta sola e non può ricevere prop. Due scostamenti a verbale — il titolo resta solo in `sr-only`, e le soglie sono `@container` sulla fascia (otto celle misurate, 1152→327px utili, **una riga sola ovunque**). → dettaglio in `WORKLOG.md`, voci **M3.2** e le due code (il `…` del percorso è un menu che si apre, azioni al kebab `⋮`; 892 scansioni / 0 violazioni) |
| M3.3 `data-table` (**2 sessioni**) | **DONE** 2026-09-11 | M2.4, M2.6 | ✔ **3 blocchi, 57 item, 916 scansioni / 0 violazioni.** TanStack Table v9 (9.2.4): colonne loro, contorno nostro; `componenti-propri.json` resta vuoto. Tastiera provata in Chromium — 1 Tab alla ricerca (500→**59** righe), 4 Tab all'intestazione, Invio ×3 e l'ordine di partenza torna. Sessione 2: il degrado è quello di shadcn — si scorre — più `bloccaPrimaColonna`, che tiene ferme casella e codice (misurato a 320px: `Nome` scivola a x=−66 mentre `Codice` resta a 66). **D17 chiusa**. Coda: intestazione su `bg-accent` (l'hover fantasma è `muted`, su `muted` sparirebbe) e `table-fixed` con larghezze nel `meta` — le colonne non saltano più su ordinamento/pagina/filtro, misurato. → dettaglio in `WORKLOG.md`, voci **M3.3 (1 di 2)**, **(2 di 2)** e code **(1)** e **(2)**, e `docs/DECISIONI.md` §33. Nuovo item `toni` (58): i quattro toni semantici come `className`, che è la forma che shadcn documenta — nessuna primitiva toccata |
| M3.4 `form-field`, `confirm-dialog`, `responsive-dialog` | **DONE** 2026-09-11 | M2.2, M2.3 | ✔ **3 blocchi, 61 item, 972 scansioni / 0 violazioni.** Il criterio è **misurato** in Chromium su sei celle: dialogo a 1440/768, cassetto a 767/375, in tutte e due le densità — la stessa chiamata, zero `if` nella story. La soglia non è nostra: è `useIsMobile()` di shadcn (768px), lo stesso pixel a cui il guscio toglie la colonna, e **non c'è una prop per spostarla**. `componenti-propri.json` resta vuoto. Dipendenza nuova: `react-hook-form`. Due difetti presi misurando — `text-left` che perdeva contro la variante del cassetto, e il fuoco del **dialogo** che esce per due stop (Base UI, identico sulle story della primitiva: non è nostro; il cassetto intrappola). → dettaglio in `WORKLOG.md`, voce **M3.4** |
| M3.5 Stati (`empty-state`, `page-skeleton`, `error-state`, `toast-con-annullo`) | **DONE** 2026-09-15 | M2.4, M2.1 | ✔ **4 blocchi, 65 item, 1016 scansioni / 0 violazioni.** `error-state` è la primitiva `empty` tinta `destructive-subtle`, non `alert`. Quarto item è il verdetto di **D18** (chiusa qui): annullo **differito lato client**, provvisorio — da rivedere con Roberto a design system finito. `componenti-propri.json` resta vuoto. → dettaglio in `WORKLOG.md`, voce **M3.5**, e `docs/DECISIONI.md` §35 |
| M3.6 `file-upload` | **DONE** 2026-09-15 | M2.2, M2.4; D8 | ✔ **1 blocco (`FileUpload` + `FileUploadList`), 66 item, 1028 scansioni / 0 violazioni.** D8: `react-dropzone` — hook puro, nessun markup proprio da disfare. Provato in Chromium: 5 file simulati, `planimetria.pdf` fallisce con errore tradotto, gli altri 4 riescono; `Tab` porta il fuoco dritto su un bottone vero (`open()` della libreria), non sulla cornice — un `role="button"` con l'`<input>` nascosto dentro dava `nested-interactive`, preso dal gate prima di arrivare qui. → dettaglio in `WORKLOG.md`, voce **M3.6** |
| M3.7 `pdf-preview` e `version-timeline` | **DONE** 2026-09-15 | M2.4; D8 | ✔ **2 blocchi, 68 item, 1044 scansioni / 0 violazioni.** D8 (seconda scelta): `react-pdf` — worker via `?url` di Vite, non CDN. PDF reale di Anagrafe (scheda tecnica FORTE CALCE, 4 pagine, in `public/esempi/`) aperto e sfogliato in Chromium, zoom 100→150% provato; un 404 sul file fa *lanciare* `<Document>` invece di passare da `onLoadError` — preso un confine d'errore React apposta. `version-timeline`: 5 revisioni finte, selezione a coppia per il confronto provata (la terza checkbox si disabilita, "Confronta" passa a → `diff-view`, M3.9 — aggiunto lì, v. coda M3.9). `componenti-propri.json` resta vuoto (blocchi, non primitive). → dettaglio in `WORKLOG.md`, voce **M3.7** |
| M3.8 `rich-text-editor` | **DONE** 2026-09-15 | M2.2; D8 | ✔ **1 blocco, 69 item, 1060 scansioni / 0 violazioni.** D8 (terza scelta): `@tiptap/react`, headless — `StarterKit` già porta `link`/`underline`, tolto `@tiptap/extension-link` come pacchetto a parte. Barra ridotta = **schema del documento**, non solo la scelta dei bottoni: titoli, citazioni, codice, riga orizzontale, sottolineato spenti nello schema — un incolla da Word non li porta dentro "declassati", ProseMirror li scarta all'analisi dell'HTML. Provato in Chromium: grassetto/corsivo/apice/elenchi funzionano, link applicato su selezione vera, incolla da Word (`paste` reale, non `setContent`) perde colore/sottolineato/titolo e tiene solo il grassetto, contatore ferma la digitazione esatto a 2048/2048 passando a `destructive`. **D11 chiusa qui**: non si adotta il generatore `typeset` di shadcn (`docs/DECISIONI.md` §36) — schema troppo ridotto per servirsene, builder non scriptabile, e D10 (l'attrito misurato in §20) non ancora chiuso. Preso un bug vero testando: `shouldRerenderOnTransaction` in corsa con la risincronizzazione di `value` cancellava ogni tasto digitato — corretto con un ref dell'ultimo valore emesso. Coda: aggiunti pedice (`@tiptap/extension-subscript`) e "Rimuovi formattazione" (`unsetAllMarks`+`clearNodes`) su rilievo di Francesco — apice e pedice non si escludono a vicenda di fabbrica, da cui il bottone. → dettaglio in `WORKLOG.md`, voce **M3.8** e coda |
| M3.9 `diff-view` e `split-view` | **DONE** 2026-09-15 | M2.4; D8 | ✔ **2 blocchi, 71 item, 1084 scansioni / 0 violazioni.** D8 chiusa: `diff` (jsdiff), zero dipendenze — `diffWords` pura, rendering nostro. `split-view` compone `resizable`; soglia a 448px **sul contenitore** (`ResizeObserver`, non viewport, `docs/DECISIONI.md` §31), provata affiancata e impilata a 375px in Chromium. Sincronia per **frazione** scorsa, non pixel — provato via `scrollTop`/`dispatchEvent` in Chromium: due colonne di lunghezza diversa arrivano insieme al fondo. Bug preso testando: `tabIndex`/`onScroll`/`elementRef` sul `ResizablePanel` finivano sul suo `<div>` esterno (`overflow: visible`), non su quello interno generato dalla libreria che scorre davvero (`overflow: auto`) — `scrollable-region-focusable`, 4 violazioni, preso da `test:a11y`; corretto mettendo la regione scorribile/a fuoco **dentro**, come `ScrollArea` in `resizable.stories.tsx`. Coda: la story `Confrontabile` di `version-timeline` mostrava ancora il segnaposto scritto prima che `diff-view` esistesse — sostituito con un `DiffView` vero (blocco non toccato, resta disaccoppiato). → dettaglio in `WORKLOG.md`, voce **M3.9** e coda |
| M3.10 Gate di fase | **DONE** 2026-09-15 | M3.1..M3.9 | ✔ `stories/PaginaProdotti.stories.tsx` (2 story, `Pagine/`) — `app-shell` + `page-header` + `data-table` + `empty-state`, zero classi `prd-*`/CSS di pagina. `test:a11y` 0 violazioni, `check` verde sui cinque gate. Coda (**D19**): `perPagina="infinito"` — scorrimento infinito come `anagrafe.tassullo.it`, testata `sticky` sul contenitore che scorre davvero (`table-container` di `ui/table.tsx`, non il riquadro), con `AppShell contenuto="riempie"`; `nomeRighe` («220 prodotti», non «220 righe»); nome-riga come varco alla scheda. → dettaglio in `WORKLOG.md`, voce **M3.10** e code, e `docs/DECISIONI.md` §37 |

## FASE 3bis — Tabelle avanzate, niko-table (10 sessioni)

Gate: `pagina-lista` mostra almeno un esempio di ciascuna nuova capacità, senza CSS di pagina; `check` verde sui cinque gate.
Dipendenze di fase: M3.3, M4.2. Si porta (si riscrive) il pattern da niko-table, non si installa il pacchetto — dettaglio in `docs/DECISIONI.md` §40.

| Attività | Stato | Dipendenze | Criterio di accettazione (sintesi) |
|---|---|---|---|
| M3bis.0 Inventario e mappa di adattamento | DONE | M3.3 | mappa in `WORKLOG.md`: bypass `DataTableRoot`/`detectFeaturesFromChildren` confermato, etichette da tradurre elencate, M3bis.5 fissato a 3 sessioni |
| M3bis.1 Righe annidate con subtotale ("Tree") | DONE | M3bis.0 | `getSottoRighe`/`CellaAlbero`/`meta.sottototale` in `data-table.tsx`; story `Albero` (computo a 2 livelli); tastiera verificata in Chromium reale (Enter/Spazio, cascata); a11y 288 scansioni/0 |
| M3bis.2 Espansione righe | DONE | M3bis.0 | `pannelloRiga`/`colonnaEspansione`/`getRowCanExpand` in `data-table.tsx`; story `Espansione` (righe escluse su `archiviato`); tastiera verificata in Chromium reale (Enter/Spazio); a11y 1156 scansioni/0 |
| M3bis.3 Resize e pin colonne generalizzato | DONE | M3bis.0 | D17 riaperta con conferma di Francesco; `ridimensionabile`/`colonneBloccabili` in `data-table.tsx`, `ui/table.tsx` **non toccato** (v. WORKLOG); story `Ridimensionabile`/`Colonne Bloccabili`; tastiera verificata (frecce, Home); a11y 1164 scansioni/0; `componenti-propri.json` resta vuoto |
| M3bis.4 Virtualizzazione / scroll infinito | DONE | M3bis.0 | `perPagina="virtuale"` (terza forma, non `"infinito"`); `@tanstack/react-virtual`; `DataTableBody`/`DataTableVirtualizedBody` estratti da `data-table.tsx`; story `Virtualizzata` (10.000 righe, mai più di ~36 `<tr>` montati); tastiera (frecce/Home/End/PageUp/PageDown) verificata in Chromium reale col fuoco che segue le righe montate/smontate; non compone con `pannelloRiga`; a11y 1168 scansioni/0 |
| M3bis.5 Data Grid editabile (**3 sessioni**: motore+clipboard/undo, celle+Zod, persistenza) | DONE | M3bis.3, M3bis.4 | Motore (`useDataGrid`/`<DataGrid>`, fuoco per cella, clipboard via `<textarea>` nascosta — non `navigator.clipboard.readText()`, assente in Safari, bug preso e corretto — riempimento, annulla/ripeti); sei celle tipizzate + `validaConZod`; `useGridChanges` (change-set puro) + `aggiungiRiga`/`rimuoviRighe`. Story `Computo`: 500 righe finte, criterio di `PIANO.md` verificato per intero. **Scarto accertato**: righe annidate (`getSottoRighe`) non compongono con la Data Grid — navigazione per indice sull'array piatto, non sul modello ad albero di TanStack. a11y 1180 scansioni/0, verificato a mano (Playwright, incl. WebKit) oltre ad axe. **Coda**: cinque rilievi di Francesco (h-full che non si risolve contro il `<td>`, anello di fuoco non edge-to-edge, colonne senza `ridimensionabile`, due difetti Safari-only sulla selezione di testo e sul fuoco della maniglia) — v. `WORKLOG.md` |
| M3bis.6 Filtri (sfaccettato, intervallo, data, reset) | DONE | M3bis.0 | Ampliata oltre il solo sfaccettato su richiesta di Francesco (pagina "Faceted Filter Table" di niko-table): quattro blocchi nuovi (`data-table-filtro-{sfaccettato,intervallo,data,reset}.tsx`) più `arrHas`/`inNumberRange`/`inDateRange` su `caratteristiche`. Due correzioni di fondo, non cosmetiche — v. `WORKLOG.md`: `barra` di `<DataTable>` passa ora anche `tabella` come secondo argomento (`tabellaRef`/`onTabellaPronta` consegnava l'istanza un render indietro); `PopoverContentFerma` tiene le tendine sempre sotto il grilletto (mai di lato, o l'ancoraggio insegue un bottone che cresce). Story `Filtri`/`Filtri Con Filtro Attivo` dentro `data-table.stories.tsx`. a11y 1188 scansioni/0 |
| M3bis.7 Drag&drop righe | DONE | M3bis.0 | `riordinabile={{ onRiordina }}` in `data-table.tsx` (`colonnaRiordino`, `RigaCorpo`, `DataTableRiordinoRighe`); nuovo prop `idRiga` (→ `getRowId` TanStack), **richiesto** con `riordinabile` — senza, l'id di riga è l'indice e dnd-kit anima un assestamento spurio sulla riga rilasciata (misurato a fotogrammi contro niko-table, v. WORKLOG); `@dnd-kit/{core,sortable,utilities,modifiers}`; ordinamento/ricerca/filtri e paginazione spenti sulla tabella mentre è attivo; story `Riordino` (colonna "Ordine" dimostrativa, caso reale: `Caratteristiche` di Anagrafe); mouse **e** tastiera (Spazio/frecce/Escape) verificati in Playwright reale — il pannello del browser dell'app non basta, v. WORKLOG; a11y 1192 scansioni/0 |
| M3bis.8 Drag&drop colonne | DONE | M3bis.0 | `colonneRiordinabili` in `data-table.tsx` (`CellaIntestazione`, `ManigliaRiordinoColonna`, `DataTableRiordinoColonne`), maniglia separata dall'intestazione (mai il `<th>` intero, per non violare `nested-interactive`); non spegne ordinamento/filtri/paginazione/virtualizzazione (a differenza di M3bis.7); story `Riordino Colonne` = resize **e** riordino insieme, come chiesto da `PIANO.md`; **coda**: `coordinateGetter: sortableKeyboardCoordinates` mancante rendeva il riordino da tastiera muto sulle colonne (passo fisso 25px, mai abbastanza per una colonna) — corretto anche su M3bis.7 (righe), dove lo stesso difetto c'era ma passava inosservato per via dell'altezza riga; tastiera e mouse verificati in Playwright reale su entrambe le story; a11y 1196 scansioni/0 |
| M3bis.9 Menu di riga condiviso, dropdown e tasto destro | DONE | M3bis.0 | `menuRiga`/`colonnaAzioniRiga`/`RowMenuItem`/`RowMenuSeparator`/`RowMenuSub`/`useDataTableRow` in `data-table.tsx`; stesso `menu` montato in `DropdownMenuContent` e `ContextMenuContent`; `enabledFor` spegne insieme tendina e tasto destro; tasto destro non compone con `perPagina="virtuale"` (scarto annotato, come `pannelloRiga`); story `Menu Riga Condiviso`; identità delle voci e tastiera (Tab→Invio sulla tendina) verificate in Playwright reale, v. WORKLOG; a11y 1200 scansioni/0 |
| M3bis.10 Editing in-riga leggero (`chiaveMemoRiga`) | DONE | M3bis.0 | `chiaveMemoRiga`/`RigaTabellaCorpo` (`React.memo`) in `data-table.tsx`; non compone con `perPagina="virtuale"` (scarto annotato); story `Editing In Riga` con contatore di render per riga (misurato: solo la riga editata avanza); Invio salva, Esc annulla, errore senza chiudere; a11y 1204 scansioni/0; **rettifica**: M3bis.0 aveva scritto che `getRowMemoKey` fosse già nel core — non c'era, v. WORKLOG; **coda** (3 rilievi di Francesco): matita/spunta/croce ancorate a destra (`-my-1 ml-auto flex`), colonna azioni `w-16`→`w-24` (la croce usciva dalla cella), riga di altezza costante fra i due stati (`-my-1` sui campi, 41px misurati in entrambi) |
| M3bis.11a Aggiornamento `pagina-lista` (registry) | DONE | M3bis.1..M3bis.10 | `PaginaListaProps` inoltra `idRiga`/`ridimensionabile`/`colonneBloccabili`/`colonneRiordinabili`/`menuRiga`, applicati alla pagina "vera" (`Con Dati`/`Poche Righe`, non una story isolata a parte, su rilievo di Francesco); tree/espansione/virtualizzazione/data-grid restano a M3bis.11b (caso "Computo", già mostrato in `Blocchi/Data Table`+`Data Grid`); `check` verde, a11y 1204 scansioni/0; **coda**: due difetti distinti sul riordino colonne — `select-none` sull'intera intestazione trascinabile (non solo la maniglia, selezione nativa del testo) **e** `CSS.Translate` al posto di `CSS.Transform` in `CellaIntestazione`/`RigaCorpo` (letto da niko-table: `Transform` porta anche `scaleX`/`scaleY` fra colonne di larghezza diversa, il vero "testo deformato durante il trascinamento"); **menu colonna confermato** (ordinamento+pin in un solo grilletto, story `Menu Colonna` in `Blocchi/Data Table`, non "in valutazione"); **rilievo indipendente**: `ManigliaRiordinoRiga` (M3bis.7) non centrata verticalmente nella cella, stesso difetto già preso per `colonnaEspansione` — corretto con lo stesso `<span className="flex items-center">` |
| M3bis.11b Aggiornamento pagina Prodotti (Anagrafe, `stories/PaginaProdotti.stories.tsx`) e gate di fase | DONE | M3bis.11a | Le quattro capacità dell'ambito confermato, tutte sulla pagina vera: resize (`ridimensionabile`) + menu colonna unico (`colonneBloccabili`, intestazioni `azioniProprie`, riscritto in loco — non importato da `data-table.stories.tsx`); menu di riga condiviso (`menuRiga`) con Modifica (`responsive-dialog`+`form-field`, scrive davvero sulla riga) ed Elimina (`confirm-dialog` controllato → `toast-con-annullo`, D18, annullabile 5s); filtri sfaccettati Famiglia/Tipo (`filterFn: 'arrHas'`), Stato resta `Select` esterno. Verificato in Chromium: resize, pin da menu, filtro con conteggio, elimina+annulla, modifica scritta sulla riga — tutti provati col dito. `check` verde sui cinque gate, `test:a11y` 1208 scansioni/0 (invariato, nessuna story nuova). **FASE 3bis chiusa**: 14/14 sessioni DONE. **Coda** (3 rilievi di Francesco): tolto il pin ridondante sulla colonna «azioni» (`azioniProprie` in `colonnaAzioniRiga`); Stato da `Select` a `toggle-group` a scelta singola; **difetto vero, due diagnosi** — con una colonna bloccata (`colonneBloccabili`) e `altezza="ferma"`, l'intestazione disegnava sopra di sé il valore della riga che in quel momento attraversava la sua fascia (riprodotto anche su Norme). Prima diagnosi (sbagliata): scroll-snap — tolti `snap-y`/`snap-proximity`/`snap-start`, ma Francesco ha verificato che il difetto si riproduceva lo stesso. Causa vera: `<th>` bloccata e `<td>` bloccata portano lo stesso `z-10` (`ancoraggioColonna`) — a z-index pari vince l'ordine nel DOM, `<tbody>` dopo `<thead>`. Corretto sulla `<TableHeader>` (`z-10`→`z-20`); **lo snap è tornato**, non aveva colpa. Verificato con `document.elementFromPoint` (non `scrollTop`, che nasconde la differenza) su otto posizioni, snap attivo e spento. Annotato non corretto: fondo di sorvolo translucido su colonna bloccata che lascia intravedere un'altra colonna scorsa sotto (preesistente da M3.3). **Coda successiva**: Stato passato da `toggle-group` a filtro sfaccettato (coerenza con Famiglia/Tipo, `filterFn` scritto a mano — `arrHas` non stringifica, un booleano non troverebbe mai un pari); menu di riga «⋯» che si staccava dalla tabella scorrendo (Base UI insegue il grilletto senza un modo di spegnerlo) — ora si chiude da sé al primo scorrimento (`MenuAzioniRiga`, stato+ref+listener su `table-container`), verificato con la rotella vera. **Coda successiva**: aggiunto `FiltroResetTutti` (dimenticato in M3bis.11b, il blocco già lo aveva da M3bis.6); il riquadro che «non torna alto disattivando i filtri» **non riprodotto** dopo prova estesa con clic veri (ricerca, sfaccettato, chip, Reset) — torna sempre pieno; da riaprire con la sequenza esatta se si ripresenta |

## FASE 4 — Pagine modello (6 sessioni)

Gate: una app Vite vuota installa `tassullo-app-shell` + `pagina-lista` + `pagina-login` e in dieci minuti è navigabile e in stile, senza scrivere layout.
Dipendenze di fase: FASE 3. Ogni pagina è un `registry:block` con dati finti tipizzati.

| Attività | Stato | Dipendenze | Criterio di accettazione (sintesi) |
|---|---|---|---|
| M4.1 `pagina-login` | **DONE** 2026-09-15 | M3.4, M3.5 | ✔ **1 pagina, 72 item.** `PaginaLogin`: logo, nome app, `onAccedi` come sola cucitura verso MSAL/Entra ID (nessuna dipendenza `@azure/msal-react`). Tre stati (`inattivo`/`in-corso`/`errore`) più `configurato` ortogonale. `check:registry` esteso a `pages/` (nuova cartella, stessa regola-3-sola dei blocchi). `check` verde sui cinque gate, a11y 278 scansioni/0 violazioni. → dettaglio in `WORKLOG.md`, voce **M4.1** |
| M4.2 `pagina-lista` | **DONE** 2026-09-15 | M3.3, M3.5, M2.6 | ✔ **2 pagine, 74 item.** `PaginaLista`: `page-header` + `data-table` + `empty-state`/`error-state`/`page-skeleton`, `stato` a tre vie (`pronto`/`caricamento`/`errore`), `vuotoIniziale` distinto dal vuoto-per-filtro di `DataTable`. Story `Pagine/Lista` su dati Norme (non Prodotti, a provare la genericità). Provata nelle **quattro celle viewport × densità** (375/1440 × normale/touch) — **chiude D10**, verdetto (a). `check` verde sui cinque gate, a11y 1128 scansioni/0 violazioni. → dettaglio in `WORKLOG.md`, voce **M4.2** |
| M4.3 `pagina-scheda` | **DONE** 2026-09-16 | M3.2, M3.4, M3.7 | ✔ **3 pagine, 75 item.** `PaginaScheda`: intestazione **visibile** (titolo + `distintivo` libero + azioni), distinta dalla fascia senza titolo di `page-header`; tab anagrafica/documenti/storico dentro `Card` (non a contatto con lo sfondo, corretto su rilievo di Francesco); `anagrafica: (modifica) => ReactNode` **un solo form sempre montato**, campi `disabled={!modifica}` — non due alberi, corretto dalla prima versione perché un campo che cambia forma al clic su "Modifica" muove l'intera scheda; `modifica`/`onModificaChange` controllabili perché il blocco non sa quando il `<form>` dell'app ha salvato; `storico` monta sempre `VersionTimeline`, `documenti` resta libero. Story su Norme (stesso dominio di M4.2). `check` verde sui cinque gate, a11y 1148 scansioni/0 violazioni. → dettaglio in `WORKLOG.md`, voci **M4.3** |
| M4.4 `pagina-dashboard` | **DONE** 2026-09-18 | M2.8, M3.3 | ✔ **4 pagine, 80 item.** `PaginaDashboard`: avvisi in cima (l'ordine del piano è quello in cui le capacità si sono pensate, non quello in cui si leggono), fila di indicatori, `grafici` come **tupla di due nodi già completi**, attività recenti sulla primitiva `table` (non `data-table`: lista corta, già ordinata, senza filtri — gradino 1 di 4bis). Zero componenti nuovi. **Un difetto muto preso in revisione**: `Card size="sm"` faceva rendere il numero dell'indicatore a **13px** invece di 27 (`CardTitle` porta `group-data-[size=sm]/card:text-sm`, che vince su `text-2xl`) — misurato in Chromium e corretto. `check` verde sui cinque gate, a11y **1224 scansioni/0 violazioni**. → dettaglio in `WORKLOG.md`, voce **M4.4** |
| M4.5 `pagina-admin` | **DONE** 2026-09-18 | M3.3, M3.4 | ✔ **5 pagine, 81 item.** `PaginaAdmin`: fascia a tab, banner fisso di **sola lettura** (non chiudibile: è lo stato dei permessi di chi guarda, non un fatto da archiviare), e — **dopo indirizzo di Francesco** — il **tab «Utenti» è del blocco**, non della story: `data-table` + menu di riga + dialogo ruoli (`ToggleGroup multiple`) + `ConfirmDialog` distruttivo, tutto montato da `utenti={{dati, ruoli, onRimuovi, onCambiaRuoli}}`. Il parallelo è `storico` in `pagina-scheda` (forma fissa, il blocco la possiede) contro `documenti` (forma libera): un utente con ruoli multipli è la stessa forma in ogni app dello studio, una tabella ruoli no. Ruoli come `string` + etichetta + tono, mai un'unione chiusa. In revisione: `colonne`/`menuRiga` messe in `useMemo` (il blocco ri-renderizza a ogni dialogo, e `data-table` documenta che colonne instabili ricostruiscono la tabella). `check` verde sui cinque gate, a11y **1240 scansioni/0 violazioni**. → dettaglio in `WORKLOG.md`, voce **M4.5** |
| M4.6 Stati di sistema e gate di fase | **DONE** 2026-09-18 | M4.1..M4.5 | ✔ **6 pagine, 82 item — FASE 4 chiusa.** `PaginaErrore` sulla primitiva `Empty` (gradino 1), quattro varianti con un tono ciascuna e `schermoIntero` **di default per variante**: 404/accesso-negato dentro il guscio, errore-server/manutenzione fuori. **Il gate ha trovato tre difetti veri, tutti corretti**: (a) **26 `registryDependencies` rotte su 12 item** (`@tassullo/data-table` invece di `@tassullo/tassullo-data-table`) — il registry pubblico era **ininstallabile** per ogni blocco con dipendenze interne; (b) due prerequisiti mai documentati (Tailwind+alias, mappatura `@tassullo` in `components.json`), ora nel `README`; (c) `process.env.NODE_ENV` in **quattro** blocchi, che rompe `tsc` in un'app Vite pura → `import.meta.env.DEV`. **Cronometro: primo giro non completabile, secondo ≈6m30s** (i tre `add` da soli 9s). `check` verde sui cinque gate, a11y **1260 scansioni/0 violazioni**. → dettaglio in `WORKLOG.md`, voce **M4.6** |
| M4.7 `item` (lista non tabellare) | **DONE** 2026-09-19 | M4.6 | ✔ **83 item.** Originale shadcn installato e **snapshot preso prima** di toccarlo; **un solo ri-stile**, ed è la solita trappola alla settima occorrenza: `[&>a:hover]:text-primary` → `text-accent-ink`. Story `Primitive/Item` in cinque scene, e il tab documenti di `pagina-scheda` passa da una `<ul>` con classi a mano alla composizione. **Il gate ha preso 8 violazioni al primo giro** (`aria-required-children`, 2×4 passate): `ItemGroup` è `role="list"` ma `Item` non si dichiara `listitem`, e il filo di `ItemSeparator` è un figlio non ammesso — `role="none"` **non basta** (porta `aria-orientation`, che lì è `aria-allowed-attr`: 2→1), serve `aria-hidden` (→0). `check` verde sui cinque gate, a11y **1280 scansioni/0 violazioni**. → dettaglio in `WORKLOG.md`, voce **M4.7** |

## FASE 4ter — Copertura delle tre app (10 sessioni)

Gate: **M4ter.10** — un'app Vite vuota installa gli otto item nuovi e compila in dieci minuti, **e** la tabella §1 di `docs/ANALISI-COPERTURA-APP.md` è ripercorsa riga per riga con l'item che la copre. Pianificata il 2026-09-19; nasce dalla gap analysis che ha chiuso D20–D23. Il perché di una fase e non di una coda alla FASE 4, con il conto, sta in `PIANO.md` §FASE 4ter.
Dipendenze di fase: FASE 4. **Numeri di partenza**: `test:a11y` **1296 scansioni** (324 story × 4), 0 violazioni; `check:registry` 0 errori / 53 avvisi / **0 componenti nostri**; **83 item**. Ogni scena di story vale 4 scansioni.

| Attività | Stato | Dipendenze | Criterio di accettazione (sintesi) |
|---|---|---|---|
| M4ter.1 `@reui`: verifiche bloccanti, i tre file, gate a due provenienze | **DONE** | D21, D22 | **Fatto tutto**: stepper (5 scene) e le due viste del calendario col motore, **10 file reui** presi da GitHub dopo un `Forbidden` di reui.io (artefatti verificati **identici byte per byte**). Gate a **provenienza per file** con 3 autotest che escono 1; normalizzazione degli import **per modulo** e non per prefisso; `MORTE_A_MONTE` per due dichiarazioni che non compilano sotto `noUnusedLocals`; `LATO_RE` contro il falso positivo `border-b-primary`; e **`shadcn view` tronca a 64 KiB in silenzio** — `--snapshot` lo rileva e ripiega sull'URL del registry. **83 → 87 item**, ri-stilati 18 → **24**, **0 componenti nostri**, a11y **1316**/0, 5 gate verdi |
| M4ter.2 `tassullo-calendario` | **DONE** | M4ter.1 | **Fatto tutto**: introduzione (`autodocs`) e **una story sola**; le scene di misura restano col tag `!dev`. Tre viste sullo **stesso periodo**, **calendario = tipo di intervento** (colore + legenda) e **assegnatari = persone** (`AvatarGroup`, più d'uno), dialogo crea/modifica/elimina col date-picker, trascinamento, menù Opzioni; il vuoto è `tassullo-empty-state`. **87 → 89 item** (entra `time-grid`: D22 passa a **11 file / 280 KB / 8 384 righe**), a11y **1332**/0, 5 gate verdi, 0 componenti nostri. **Trovato un falso positivo del gate**: la normalizzazione degli import di M4ter.1 non mordeva — ri-stilati 25 → **19**. → `docs/DECISIONI.md` §44 |
| M4ter.3 `entity-image`, il primo componente nostro | **DONE** | D20 | **Fatto tutto**: 6 scene, **prima riga** di `componenti-propri.json` (0 → **1**) e due autotest che escono 1 (riga tolta, riga senza `approvatoDa`). Larghezza di cella **256px** = `--container-3xs`, misurata in Chromium su 5 candidate e sulle larghezze vere del guscio (343/960/1120). **Tre rapporti e non due**: `1:1` è entrato su rilievo di Francesco perché **tutta** la libreria Tassullo è quadrata (sistemi 1080², prodotti 1800²) e a `4:3` si perde il 25% dell'altezza. **Due difetti muti**: Base UI scrive `alt=""` su ogni `<img>`, quindi axe **non** vede una foto senza alt — la CI ha preso un passo `npm run build`; e Tailwind v4 emette solo i token usati da un'utility, quindi `var(--container-2xs)` cadeva in silenzio. Immagini d'esempio **vere e scontornate** (PNG con alpha, 0 px di frangia): da lì è uscita anche la regola che **un componente non dipinge un fondo che non gli è stato chiesto**. **89 → 90 item**, a11y **1356**/0, 5 gate verdi. → `docs/DECISIONI.md` §45 |
| M4ter.4 `pagina-login` prende `modo` | **DONE** 2026-09-20 | D23 | Tre vie, con **due interruttori** (`configurato` invariato per l'SSO, `credenzialiAbilitate` per la via locale) e `statoDi` che dice a quale via appartengono `errore` e `in-corso`. **Non-regressione: 0 differenze** di DOM su **8 file su 10** (5 scene × 2 modalità), contro una linea di base presa su `main` prima di toccare; le 2 restanti sono **una virgola in un nodo di testo** della scena `Errore`, tolta su conferma esplicita di Francesco — stesso albero, stesse classi, ed è una stringa che passa la story, non il componente. Firma a **unione discriminata**: `modo="entrambi"` senza il suo gestore non compila (provato). Logo Microsoft come nodo dall'app, zero SVG di terzi nel registry. a11y **1376**/0 su 344 story, `check:contrast` 48/48, **90 item** invariati, `check` verde sui **sei**. → i quattro punti di `docs/SPEC-AUTH.md` §3 sono ora «risolti così». Due code: il sesto gate `check:registry-build` (che ha trovato una deriva vera alla prima esecuzione) e l'avviso dell'SSO spostato accanto al suo bottone, su rilievo di Francesco |
| M4ter.5 Le cinque schermate d'accesso di Studio | **DONE** 2026-09-20 | M4ter.1, M4ter.4 | Sette scene in `stories/SchermateAccesso.stories.tsx` (`Pagine/Schermate d'accesso`) — non accanto a `pagina-login`, perché non ne esercitano una prop. **Zero componenti nuovi**: **90 item** e **1 componente nostro**, invariati; `check` verde sui sei, a11y **1404**/0 su 351 story, bersagli 3092 con 0 piccoli in entrambe le direzioni. Due affermazioni della spec cadute e riscritte: `Field orientation="responsive"` **non scatta mai** in `Card max-w-sm` (`FieldGroup` 352px contro 448 di soglia, misurato) → registrazione a `max-w-lg`, scelta di Francesco; e `PaginaLogin` non esporta il guscio, quindi si **ricompone**. Marchio centrato (`justify-self-center`): differenza voluta, **una classe su un elemento**, 20 file su 20, zero altre differenze. → `docs/SPEC-AUTH.md` §1.3, §2, §2.1 riscritta. **Coda 1**: i requisiti della password diventano un controllo dinamico su tre rilievi di Francesco — codice invito da `defaultValue` a `placeholder`, elenco che si accende mentre si scrive, riscontro che le due password coincidano. Regole **NIST 800-63B / OWASP** (lunghezza + screening), non di composizione: `Rossi2026!!!` le passerebbe tutte ed è la peggiore password possibile per `mario.rossi@…`. Trovato un difetto vero: la coincidenza stava in un `else if` e una password corta nascondeva la ripetizione sbagliata. **Coda 2**: su riconferma di Francesco entrano anche le regole di composizione (**sei** regole) e un **misuratore di robustezza** — che resta codice di pagina, non un componente, quindi la decisione del 2026-09-19 regge. «Non è fra le password più usate» diventa **asincrona a quattro stati**, perché in locale non si verifica: il modo vero è HIBP con k-anonimato, scritto in §2.1ter. Il misuratore è limitato a «media» finché un requisito manca — `tegola-ponte-neve` dava «forte» con 4/6. Elenco su **due colonne**; «Reimposta la password» passa a `max-w-lg`. §2.1ter |
| M4ter.6 `useSoglia` e la lista a due facce | **DONE** 2026-09-20 | M4ter.3 | **Il primo render è provato, non dedotto**: `MutationObserver` da `addInitScript`, Chromium a 375px — la `<table>` **non compare mai** (0 su 280 mutazioni); lo stesso strumento su `useIsMobile` la vede entrare e uscire. **Strada (a) di §46**: le facce si rendono da una prop della **pagina**, e quella che ogni scena rende **nel gate** è misurata. **La soglia NON si deriva dalla tabella** — due rilievi di Francesco hanno fermato 1280 e poi 1536 (più di un portatile 13"): vale `ANALISI-COPERTURA-APP.md` §6.3, «si decide app per app». Default **1024** (Officina), una scena a **900** (RadarOpere). **Tre difetti presi da Francesco a video e da nessuna misura**: con `table-fixed` la colonna **elastica va a 0px** e le intestazioni si sovrappongono (→ *ogni* colonna dichiara la sua larghezza, tabella minima **1160**, sotto scorre); la miniatura era **tagliata** perché dimensionata sulla colonna e non sullo spazio utile (`w-16`→`w-12`); `bloccaPrimaColonna` bloccava il **chevron** e ci disegnava accanto un separatore — tolta. `check:registry` impara `hooks/`, regola 3 sola, `componenti-propri.json` resta a **1**; autotest in 4 direzioni. **90 → 91 item**, a11y **1416**/0, bersagli **3171** con 0 piccoli, sei gate verdi. → `WORKLOG.md` M4ter.6 |
| M4ter.7 `tassullo-indicatori` e le due prop di forma | **DONE** 2026-09-20 | M4.4, M4.3, M3.2 | **La trappola era vera e l'ho misurata prima di correggerla**: la fila portava `@4xl/dashboard`, e spostata fuori dal contenitore della dashboard rendeva `628px ×2` su una riga da 1272 — due colonne per sempre, senza errore e con axe verde. Il blocco dichiara ora il **proprio** `@container/indicatori`: fuori dalla dashboard `340px ×4`, dentro gli stessi `276px ×4` di prima. Numero riletto dal DOM: **27px** sotto i 250 di carta, **31** sopra, etichetta 13 — il difetto muto di M4.4 resta chiuso. **Nessuna copia**: `RigaIndicatori` non esiste più, e lo scheletro del caricamento monta `GrigliaIndicatori`, una sorgente sola. Non-regressione **8 alberi su 8, 0 differenze** oltre il nodo contenitore — e la prima misura mentiva (36 coordinate Recharts sfasate di ≤0.085px) perché catturavo a 200ms, con l'animazione delle barre ancora in corso: §32 vale anche su `innerHTML`. `titolo` → `ReactNode` e `descrizione` su `pagina-scheda`. **91 → 92 item**, a11y **1452**/0 (7 scene, non 6: il confronto delle due forme del contatore in una scena sola dava `landmark-no-duplicate-banner`), bersagli **3199** su 363 story con 0 piccoli, sei gate verdi. **Coda, tre difetti di Francesco a video:** la legenda del grafico tagliata (`flex-wrap`, sole classi), la tabella delle attività che non degradava (prop `faccia` + `useSoglia` 768, faccia stretta a elenco — il difetto 1 di M4ter.6 su un'altra tabella) la fascia morta sotto le card dei grafici (`h-72` rimesso a `h-64`) e il **pavimento a una colonna sotto `@sm`** — su rilievo suo: sotto i 384px di contenitore la carta va sotto i 200px e un valore lungo **sborda dalla `Card`** (`overflow-hidden`, quindi si taglia); la sonda ingenua non lo vedeva perché il titolo va a capo invece di sbordare. → `WORKLOG.md` M4ter.7 |
| M4ter.8 Piede di tabella, conferma digitata, chip col conteggio | **DONE** 2026-09-20 | M3.3, M3.5 | ✔ **92 item (invariati), a11y 1472/0, sei gate verdi.** Il piano sbagliava un fatto: i «due rami» sono i due *corpi*, il `<Table>` è uno — `<tfoot>` scritto una volta, `sticky bottom-0`. Il piede **segue il filtro**, letto dal DOM: 15 righe `106,38`/`2086,93 €` → filtro «03.05» 4 righe `24,66`/`855,70 €`; sul corpo virtualizzato 4000 → 1068 righe. Il campo della conferma è **fuori** da `aria-describedby` (letto dal DOM) e `onConferma(valore)` porta fuori il digitato. Difetto preso a video e non da un gate: «Totale — 1…» troncato, le celle del piede ora si fondono. Non-regressione 58 alberi, 0 differenze; bersagli 3228 su 368 story, 0 piccoli. **Coda**: il separatore delle migliaia sempre (`useGrouping: 'always'`) su rilievo di Francesco — in it-IT il CLDR non raggruppa le quattro cifre, quindi il punto era *intermittente*; **D chiusa, `docs/DECISIONI.md` §47**, e il difetto era già spedito su `Lista a due facce` (4 celle su 5 senza punto). Su indirizzo di Francesco la convenzione è diventata un item — **`numeri`, 92 → 93**, sul modello di `toni`: `intero`/`decimale`/`valuta` più `formattatore`, in scena nella sezione 7 di `Tema/Cifre`. Resta scoperto `chart.tsx` (`toLocaleString()` senza locale, codice shadcn: la via è `formatter` sul `chartConfig`). **Seconda coda**: **`font-mono` sospeso sui codici** (Roberto, «per ora» — `docs/DECISIONI.md` §48), 14 punti d'uso ripuliti più 2 che erano già contro la regola vecchia (numeri in mono). Costo misurato: in Inter `O`/`0` si distinguono, `I`/`l` no — ma i codici Tassullo sono maiuscoli. → `WORKLOG.md` M4ter.8 |
| M4ter.9 `tassullo-barra-contesto` | TODO | M4.7 | Composizione `item` + `dropdown-menu`; `Esc`, clic-fuori e ritorno del fuoco misurati **in Chromium vero**; icona Lucide, **niente emoji** (stessa ragione di D19). **93 item**, a11y **1472**/0 |
| M4ter.10 Gate di fase | TODO | M4ter.1..M4ter.9 | Installazione **cronometrata** (confronto coi 6m30s di M4.6), compresi gli item reui in un'app che **non dichiara `@reui`**; `registry validate` su **93 item**; `check` verde sui cinque, a11y **1472**/0; `misura:bersagli` col controllo dello strumento. Tabella §1: dodici righe «da fare» → ognuna con l'item che la copre |

## FASE 5 — Registry, distribuzione e guida di adozione (6 sessioni)

Gate: l'installazione in un'app vergine produce un'app in stile Tassullo che compila — **verificata in locale, prima di qualunque pubblicazione** — seguendo solo i documenti scritti qui.
Dipendenze di fase: FASE 4.

| Attività | Stato | Dipendenze | Criterio di accettazione (sintesi) |
|---|---|---|---|
| M5.1 `registry.json` completo | TODO | FASE 4 | `registry validate` verde e `list` elenca tutti gli item attesi, con `target` e `registryDependencies` corretti |
| M5.2 Prova d'installazione end-to-end | TODO | M5.1 | app Vite usa-e-getta compilata e in stile dal registry **locale**; lista degli attriti d'uso in WORKLOG |
| M5.3 `INTEGRAZIONE.md` v2 (app nuova) | TODO | M5.2 | seguendo **solo** il documento si porta un'app Vite vuota a una pagina in stile; tempo effettivo in WORKLOG |
| M5.4 Regole per `CLAUDE.md` e piani delle app | TODO | M5.3 | blocco di regole copiabile senza adattamenti; `DECISIONI.md` motiva no-npm, Base UI, `primary`≠`accent`, oklch, font non distribuito |
| M5.5 `GUIDA-MIGRAZIONE.md` (app esistente) | TODO | M5.4, **FASE 4ter** | **passo 0 eseguito davvero su Anagrafe** (solo `components.json` + `.mcp.json`, zero modifiche a codice e stili); guida verificata a secco sulle sue 10 pagine. **Raccoglie le «Note per M5.5»** in coda a `docs/ANALISI-COPERTURA-APP.md` — cose che il design system non può risolvere da sé, come `EventoCalendario.programmato_il` di Officina, che è un istante dove serve inizio e fine. Resta aperta la divisione M5.5a/M5.5b (Anagrafe è il **9%** del CSS delle tre app): si decide affrontando la FASE 5 |
| M5.6 Pubblicazione su GitHub + Pages | IN_PROGRESS | M5.5 | **Repo online e Pages attivo dal 2026-09-10** (D4 chiusa e anticipata). Restano: tag `v2.0.0` e install da GitHub riuscita fuori dal repo. La storia è **già stata ripulita** il 2026-09-09 (i font in base64 tolti da tutti i commit con `git-filter-repo`) → dettaglio in `WORKLOG.md` |

## Decisioni

| Decisione | Stato | Blocca | Nota |
|---|---|---|---|
| D2 valori della palette scura | **CHIUSA** 2026-09-07 | — | chiusa guardandola affiancata al chiaro, non al buio. Neutri dalla sidebar v1, brand invariato, tenui specchiati a gradini fissi; tre scostamenti misurati (`--sidebar`, `--info`, `--warning`). Motivazione in `WORKLOG.md` e `PIANO.md` §2bis |
| D3 come si servono i font alle app | **DONE** (2026-09-08) | nessuno | **Chiusa**: Inter va nel registry, item `tema-font`, coi file in **data URI** dentro il CSS — perché `shadcn build` legge i file come testo e un `.woff2` arriverebbe corrotto senza errore (73.016 byte → 69.186 caratteri, misurato). → dettaglio in `docs/DECISIONI.md` e in `WORKLOG.md` |
| D4 quando pubblicare su GitHub | **CHIUSA** 2026-09-10 — **subito, pubblico** | — | Forzata più che scelta: org su piano **free**, dove Pages vive solo sui repo pubblici, e la scorciatoia `owner/repo/item` della CLI shadcn è documentata solo per il pubblico. Privato avrebbe messo un token GitHub in ogni app, CI ed editor. Tolto dal repo `public/fonts/` per intero (nota Lineto). → `WORKLOG.md`, voce **D4** |
| **D20** il primo componente nostro: l'immagine d'entità con segnaposto | **CHIUSA** 2026-09-19 — **sì, come componente**, rapporto `4:3` di default e `16:9` disponibile | **M4ter.3** | Sarà la **prima riga di `componenti-propri.json`**, finora vuoto. `avatar` non regge (tondo, max 40px, fallback testuale), `ItemMedia` dà 40/32/24px, `aspect-ratio` dà il solo rapporto; shadcn non ha un componente immagine. Prova della lacuna: `MacchinaIcon` scritta **identica in 4 file** di Officina. Scartate due strade: sovrascrivere `avatar` dal punto di chiamata (5 classi da annullare in ogni pagina) e aggiungere una variante a `avatar.tsx` (vietato da 4bis, ed è la divergenza cara). **Scritta in M4ter.3**: rapporto e larghezza di cella (**256px**) misurati. → `docs/ANALISI-COPERTURA-APP.md` §4, `docs/DECISIONI.md` §45 |
| **D21** il registry `@reui` si dichiara in `components.json`? | **CHIUSA** 2026-09-19 — **sì**, dichiarato | **M4ter.1**, e da lì M4ter.2 e M4ter.5 | Una riga, additiva e reversibile, **non implica installare niente**. Vale oltre il calendario: l'MCP legge i soli registry dichiarati, quindi «l'ho chiesto all'MCP» significava «l'ho chiesto a `@shadcn`». È il motivo per cui il calendario a eventi risultava introvabile. Verificato che `shadcn view @reui/…` risponde. → `docs/ANALISI-COPERTURA-APP.md` §3 e §6.11 |
| **D22** il calendario a eventi: si scrive o si adotta? | **CHIUSA** 2026-09-19 — **si adotta `@reui/event-calendar`** | **M4ter.1**, con tre verifiche bloccanti: se una non regge, questa decisione **si riapre** | Ribaltata da due rilievi di Francesco: l'esistenza di reui (**Base UI, preset `base-nova`, il nostro identico `style`**) e il fatto che **un fermo macchina può durare giorni** — cioè servono le barre multi-giorno, il calcolo delle corsie, la parte cara. Licenza: i **Pro Blocks** vietano la pubblicazione in repo pubblici, ma il **componente** è in `keenthemes/reui`, **MIT** — avviso di copyright da conservare. Non è il caso di niko-table: lì mancava `shadcn view`, qui funziona, quindi `check:registry --snapshot` copre anche reui. → `docs/ANALISI-COPERTURA-APP.md` §6.11 |
| **D23** le vie d'accesso di `pagina-login` | **CHIUSA** 2026-09-19 — **`modo: "microsoft" \| "credenziali" \| "entrambi"`**, default `microsoft` | **M4ter.4**, e M4ter.5 per le altre schermate | Studio ha utenti **fuori dall'azienda** (liberi professionisti, imprese): l'SSO puro è impossibile, non sconveniente. Insieme chiuso invece di una sezione facoltativa, perché su `/login` di Studio l'SSO è il caso **secondario**, in fondo dopo il separatore — e una prop facoltativa non lo renderebbe dicibile. Il default tiene **Anagrafe e Officina immutate**. Cade con questa anche il «wizard» come componente: i tre passi della registrazione compongono con `field`+`radio-group`+`checkbox`, manca la sola barra a segmenti. → `docs/SPEC-AUTH.md` |
| D19 `native-select` di shadcn entra nel registry? | **CHIUSA** 2026-09-19 — **no** | nessun task: gli 81 `<select>` a mano passano al `select` esistente, in **M5.5** | Aperta dall'analisi delle primitive mancanti (coda di M4.6): 81 `<select>` scritti a mano nelle tre app (Officina 46, Studio 23, Anagrafe 12), il numero più alto emerso. Verdetto di Francesco: **no**, perché la tendina nativa la disegna il **sistema operativo** e su Windows non salta alla voce premendo una lettera, mentre su macOS sì — un componente che si comporta diversamente a seconda della macchina è l'opposto di un design system. È la stessa ragione per cui il `select` Base UI era stato scelto: parità fra sistemi. Verificato sul nostro: `p`→Pubblicato, `a`→Archiviato, `i`+`n`→In revisione. → `docs/DECISIONI.md` §42 |
| D18 conferma o annullo, per quali azioni | **CHIUSA** 2026-09-15 — **differita lato client, provvisorio** | M3.5 (`DONE`) | Aperta da Francesco su `confirm-dialog`: «un alert o sonner con la possibilità per l'elimina di annullarlo?». Non sono complementari, sono **alternative**. Verdetto: l'annullo è **differito lato client** — nessun contratto di soft-delete richiesto al backend — e va rivisto con Roberto a design system finito. Costruito come `tassullo-toast-con-annullo` in M3.5. → `docs/DECISIONI.md` §35 |
| D17 resize delle colonne da parte dell'utente | **CHIUSA** 2026-09-11 — **no per ora**, col grilletto scritto | riapre alla prima tabella in cui dichiarare le larghezze non basta | Aperta da Francesco: «non abbiamo idea di che tabelle svilupperemo in futuro, potrebbe diventare necessario **limitatamente a desktop**». TanStack v9 ce l'ha (`columnSizingFeature` + `columnResizingFeature`); shadcn no — la maniglia è **gradino 4**. Chiusa da Francesco: il problema che il resize risolve — colonna troppo stretta — lo risolve già `meta.larghezza`, a costo zero, e la maniglia sarebbe il **primo** componente nostro in un registro vuoto. **Verificato che nulla la preclude**: `table-fixed` è un prerequisito del resize, non un ostacolo. → `docs/DECISIONI.md` §34, coi quattro costi già scritti per chi riaprirà |
| D5 destino di `components.css` e `stylelint-config.cjs` v1 | TODO | nessuno | fuori dal perimetro: si dismettono a ultima app migrata |
| D6 quando migrano Anagrafe / Studio / Officina | TODO | nessuno | ognuna decide per sé con la guida M5.5; nessuna scadenza |
| D7 se `docx/` resti nel repo v1 | TODO | nessuno | nessuna urgenza, mai |
| D8 librerie di terze parti per i blocchi | **DONE** 2026-09-15 | M3.6..M3.9 | Le quattro: `react-dropzone` (M3.6), `react-pdf` (M3.7), `@tiptap/react` (M3.8), `diff` (M3.9) — tutte hook/funzione pura, nessun markup di libreria da disfare, sostituibili senza toccare l'API del blocco |
| D12 `next-themes` va tolto da `sonner`? | **CHIUSA** 2026-09-09 — **no, si lascia** | nessuno | Aperta e chiusa in giornata, in M2.3. `sonner.tsx` importa `useTheme` da `next-themes`, che non usiamo: la modalità la mettiamo come classe sulla radice. → dettaglio in `docs/DECISIONI.md` e in `WORKLOG.md` |

| D13 il marchio Tassullo come item del registry | **CHIUSA** 2026-09-10 — **(c) `mask-image` nel CSS del tema** | **entro la fine della FASE 2** (M2.9) | Aperta in M2.5. **L'asset non manca più**: Roberto ha segnalato che Anagrafe ha il marchio in produzione da tempo — `frontend/public/tassullo-t.svg`, un `path` solo con due sottotracciati, `viewBox` 24×38, `fill-rule="evenodd"`. → dettaglio in `docs/DECISIONI.md` e in `WORKLOG.md` |
| D16 la scala tipografica è due punti sotto shadcn | **CHIUSA E ATTUATA** 2026-09-10 — **«+1» su ogni gradino, `md` fuso in `sm`, `title` eliminato** | attuata in **M1.6** (`DONE`) | Aperta in M3.1 da Francesco, confrontando la style guide coi blocchi di `ui.shadcn.com`. → dettaglio in `docs/DECISIONI.md` e in `WORKLOG.md` |
| D14 i bottoni-icona del combobox senza nome accessibile | **CHIUSA** 2026-09-09 — **si accettano, non si diverge** | nessuno | Aperta e chiusa in giornata, in M2.6. Il chevron che apre l'elenco e la crocetta di ogni pillola portano solo l'icona: axe dà `button-name`, gravità *critical*, 16 violazioni. → dettaglio in `docs/DECISIONI.md` e in `WORKLOG.md` |
| D11 il testo lungo: si adotta `typeset` di shadcn? | **CHIUSA** 2026-09-15 — **no** | attuata in **M3.8** (`DONE`) | Verdetto: non si adotta. Il builder non è uno script (non rientra nel pattern `check:font`/`check:logo`), il caso reale di M3.8 (`rich-text-editor`) ha uno schema troppo ridotto per servirsene, e la leva responsiva di `typeset` si compone con la densità touch in un modo che D10 (M4.2) non ha ancora giudicato. Si riapre se un consumatore concreto di testo lungo vero lo chiede. → `docs/DECISIONI.md` §36 |
| D10 la densità touch su schermo stretto | **CHIUSA** 2026-09-15 — **(a) si tiene così com'è** | attuata in **M4.2** (`DONE`) | Verdetto preso sulla pagina più densa del registry (`pagina-lista`, filtri + tabella + paginazione): a 375px × touch la colonna resta **327px** utili, la stessa cifra di M3.1 — nessun difetto nuovo introdotto dalla densità della pagina, nessuna riga bersaglio sotto soglia. Non si scorpora `--space-page` (uscita b) né si riduce il fattore sotto una larghezza (uscita c, scartata a monte perché reintroduce una dipendenza dal viewport). → dettaglio in `WORKLOG.md`, voce **M4.2**; misura precedente in `docs/DECISIONI.md` §29 |
| D15 la tastiera del calendario non naviga | **CHIUSA** 2026-09-09 — **si accetta, non si diverge** | nessuno | Aperta e chiusa in giornata, in M2.7. → dettaglio in `docs/DECISIONI.md` e in `WORKLOG.md` |
| D16 quale set d'icone | **CHIUSA** 2026-09-10 — **si tiene Lucide** | nessuno | Scelta di Francesco fra i cinque set che la CLI shadcn sa installare (Lucide, Tabler, Phosphor, Remix Icon, HugeIcons), confrontati sulle 39 icone che le app useranno davvero. → dettaglio in `docs/DECISIONI.md` e in `WORKLOG.md` |

Accertamenti tecnici già chiusi sul campo (con la prova): **`docs/DECISIONI.md`**, nato in M0.2. Dove contraddice `PIANO.md`, vince lui e la contraddizione è annotata.

Chiuse il 2026-09-07 (registro in `PIANO.md` §4): **D1** — nome `tassullo-design-system-v2`; **D9** — libreria di primitive **Base UI**, con unica eccezione possibile sul calendario (M2.7), da motivare in WORKLOG.

## Candidati sospesi

Cose che **non** sono a piano, scritte qui con l'**innesco** che le riapre. Non è un elenco di desideri: un candidato non scritto è un candidato che si riscopre da zero, ed è già successo con `native-select`, analizzato due volte prima di diventare D19.

| Candidato | Stato | Innesco che lo riapre | Nota |
|---|---|---|---|
| **Una tavolozza estesa (10–15 colori) per grafici, calendario e categorie** | SOSPESO 2026-09-19, **da decidere in coda alla FASE 4ter** | è già innescato **due volte**: Officina usa il **rosso** per «Guasto» e nei cinque `--chart-*` il rosso non c'è; e in M4ter.5 il misuratore di robustezza della password ha trovato che **manca un fondo per la banda intermedia** — `--warning` è il crema pallido del badge (#FBE8C4) e come riempimento di una barra sparisce sul fondo della card, quindi le tre bande sono `destructive`/`primary`/`success` e il quarto gradino non esiste | Oggi la famiglia per distinguere serie è `--chart-1..5` — arancio, verde, blu e **due grigi vicini**, cioè quattro tinte utili su cinque. Basta per un grafico, non per un calendario a categorie: M4ter.2 ha dovuto dare l'arancio al «Guasto» perché il rosso non esiste. Il lavoro non è aggiungere cinque righe: la palette è **una sola costante** in `scripts/hex-to-oklch.ts`, e ogni tinta nuova va in **chiaro e scuro** e passa da `check:contrast` (48 coppie oggi, che diventerebbero il doppio). Da cui la domanda vera, che è di sistema: le tinte di categoria hanno bisogno del contrasto 4.5:1 come i token di testo, o bastano a fondo e si misura il testo sopra? → `docs/DECISIONI.md` §44 (m) |
| **Lo slot in `app-shell`** per una fascia persistente fra la testata e `<Outlet/>` | SOSPESO 2026-09-19 | **quando una seconda app ha un contesto attivo che attraversa le pagine** (un impianto, una famiglia, un cantiere) | Oggi ce l'ha il solo Studio, su 5 pagine, e la pagina **monta la barra da sé e funziona**: il blocco `tassullo-barra-contesto` (M4ter.9) copre il bisogno. Quello che manca allo slot è la sola garanzia di non dimenticarsi una pagina — che con un consumatore solo non vale un'API in più nel guscio. → `docs/ANALISI-COPERTURA-APP.md` §1, riga dei differiti |

## Assunzioni sotto osservazione

| Assunzione | Nota |
|---|---|
| A1 le app consumer restano su Vite + React + TypeScript | se cambia, cambia il target dei `registry:block` |
| A2 Tailwind v4 accettabile in tutte le app | introduce una build-dependency che il v1 non aveva |
| A3 taratura delle sessioni per analogia con Anagrafe | si verifica sul consuntivo, fase per fase |
| A4 esisterà una **app nuova** su cui il v2 debutta | se tarda troppo, anticipare una pagina di Anagrafe come collaudo (riapre D6) |
